# Очередь основателя: посмотреть позже

> Живой список того, что требует взгляда основателя, но не блокирует текущую
> работу. LLM-сессии **добавляют** сюда пункты (с датой и ссылкой), основатель
> **вычёркивает** просмотренное (удаляет строку или помечает решение рядом).
> Это не бэклог задач (он в issues) и не архитектурные обсуждения (они в
> DISCUSSION_LOG.md) — только «прочитай и согласись/возрази».

## Продуктовые решения, принятые LLM от твоего имени — проверь

Пока реализация не началась, несогласие правится дёшево (follow-up PR).

- [ ] **Веб-чат по QR — реализация (27.07, spec 0027, PR #111–#114)** — решения UX/операционки, принятые при реализации #79:
  - [ ] **Формат кода заселения**: 6 символов без похожих букв (`K7M-9QT`), печатается CLI один раз при заселении; ресепшен переписывает/показывает гостю с карточки. Терпимый ввод (регистр/дефисы не важны).
  - [ ] **Заселение до кабинета — через CLI на сервере**: `python -m hospitality.tools.checkin 101 --guest "Имя" --nights 2` (перевыпуск `--reissue`, выезд `--check-out-now`, список `--list`). Кнопка «заселить» придёт с кабинетом v1 (#48).
  - [ ] **Контент QR в номере**: `https://staging.necturn.com/g/demo-hotel/<номер-комнаты>` (в проде — прод-домен). Печать QR — твой операционный шаг; сгенерировать можно любым QR-генератором.
  - [ ] **Текст согласия на обработку ПД** на кнопке входа — черновик v0 (одна строка en+ru); финальные kk/ru/en тексты заменит юрпакет. Версия согласия пишется в БД на каждую привязку.
  - [ ] **Телефон ресепшена** в статическом ответе неавторизованным берётся из конфига тенанта (`reception_phone`) — сейчас у demo-hotel НЕ заполнен, надо задать перед пилотом (частично закрывает пункт «Формат статического ответа» ниже; язык ответа — статичный en+ru, per-tenant тексты — не сейчас).
  - [ ] **Один активный Stay на комнату**: второе заселение занятой комнаты CLI отвергает — сначала выезд. Просроченные, но не выписанные проживания CLI помечает «ПРОСРОЧЕН».
- [ ] **Напоминание о невзятой заявке (27.07, issue #57, spec 0028)** — решения, принятые при реализации SLA-lite. ⚠️ **Три пункта ниже основатель пересмотрел в обсуждении 27.07 (ответы на Q5/Q6 операционного аудита) — их надо внести в spec 0028:** (1) добавляется вторая ступень — если после напоминания заявку не взяли в течение того же срока категории, одно сообщение уходит в дефолтный staff-чат «уровнем выше» (адрес уже даёт spec 0026; персональная эскалация по-прежнему не делается — нет `User`; если у категории нет своего чата и заявка изначально ушла в дефолтный, ступень пропускается); (2) интервал до эскалации равен сроку категории — отдельной настройки не заводить; (3) добавляется второй порог «взяли и не закрыли» (`in_progress`) тем же сканом, отдельным полем конфига и своим ключом идемпотентности, но **по умолчанию выключенный** — у `new` тишина всегда дефект, у `in_progress` может быть нормой (починка кондиционера 4 часа — законно).
  - [x] **Напоминания включены по умолчанию у всех тенантов: 30 минут** — подтверждено основателем 27.07. Сегодняшняя тишина и есть дефект (#57), поэтому отель, который ничего не настроил, получает защиту сам; выключение — явное (`--off`). Практическое следствие первого прогона на staging: по каждой давно висящей заявке уйдёт по одному напоминанию в staff-чат. Если 30 минут для твоего пилота не тот порог — правится одной командой: `python -m hospitality.tools.request_reminders --after-minutes 20`.
  - [x] ~~**Одно напоминание на заявку, без повторов и без эскалации «уровнем выше».**~~ **Пересмотрено 27.07:** одно напоминание в чат службы остаётся (повторов нет), но добавляется вторая ступень — сообщение в дефолтный чат. Довод основателя и сессии: находка на staging была не «уведомление не пришло», а «пять заявок висят двое суток в чате, который никто не открыл»; одно повторное сообщение туда же — это два непрочитанных вместо одного. Эскалация **в чат** владельца не требует — требует адреса, который spec 0026 уже даёт (в отличие от персональной эскалации «менеджеру лично»).
  - [x] ~~**Заявки в работе (`in_progress`), висящие сутками, НЕ подсвечиваются.**~~ **Пересмотрено 27.07:** механизм делается тем же сканом сейчас, порог по умолчанию пустой. Довод: для гостя «взяли и забыли» хуже, чем «не взяли» — ему уже пообещали; напоминать можно тому же чату службы, владелец для этого не нужен. Живой случай — заявка комнаты 101 на staging, пятые сутки в работе.
  - [ ] **Свой срок на категорию** (`maintenance=10 housekeeping=45`) доступен сразу — уборка ≠ прорыв трубы; по умолчанию не задан.
  - [ ] **Напоминание идёт в чат СВОЕЙ службы** (тот же маппинг, что уведомления, spec 0026), с кнопками «Взять в работу»/«Отменить» — реагировать можно одним нажатием.
- [ ] **ADR-008 (27.07, PR #109)** — [полный текст](adr/ADR-008-identity-and-access.md). Ключевые решения, которые стоит осознанно подтвердить:
  - [ ] **Строгий auth-only до привязки**: неавторизованный гость получает статический ответ без LLM и не может создавать заявки — никаких «пары публичных вопросов» в Phase 1 (это разноска твоего решения 22.07, но теперь оно в камне).
  - [ ] **Код заселения без запасного канала**: SMS/email-доставки нет, только код от ресепшена. Гость без кода = неавторизованный.
  - [ ] **Нет grace-периода после выезда**: checkout мгновенно гасит доступ; забытые вещи и претензии — через ресепшен, не через бота.
  - [ ] **Перевыпуск кода как восстановление**: «гость потерял код» → ресепшен перевыпускает из карточки Stay, старый код гаснет; показать старый код повторно нельзя (в БД только хэш).
  - [ ] **Код многоразовый в пределах проживания**: семья/второе устройство привязываются тем же кодом.
  - [ ] **Продление/ранний выезд правят Stay, не токен**: отдельного действия «продлить доступ гостя» у персонала не будет — только правка дат проживания.
- [ ] **ADR-006 + юрпакет #126 (28.07, PR #130/#131)** — дефолты, принятые при написании; проверь (лучше вместе с юристом до публикации):
  - [ ] **Политика честно декларирует имя гостя** (`guests.display_name` появился с guests/ 27.07) — текст issue «не собираем имена» устарел; собираем: имя при заселении, комната+период, id чата, тексты сообщений.
  - [ ] **Сроки в DPA-шаблоне**: уведомление отеля об инциденте ≤24 ч; смена субоператоров — уведомление за 10 раб. дней; экспорт+удаление при расторжении — 30 дней; записи о согласии — 3 года после договора. Цифры — здравый смысл LLM, не норма закона.
  - [ ] **Уведомление КИБ при утечке подаёт платформа** (как оператор), копия отелю — распределение ролей в DPA N.6; юрист может решить иначе (подача собственником).
  - [x] **Consent-gate при неизвестном языке показывает все три версии** (kk+ru — минимум по Закону «О языках», en — демография); черновик v0 веб-чата заменён финальными текстами (#127, spec 0029).
  - [ ] **Отдельная пара ключей age для прода** (ADR-006 §4) — вторая запись в твоём менеджере паролей, когда появится прод-контур.
  - [ ] **При полном удалении по запросу гостя** записи о согласии и `guest_identities` сознательно остаются (доказательство законности обработки) — runbook subject-requests §3.3.
- [ ] **Разноска стратсессии 27.07 в план и issues (28.07, этот PR)** — решения, принятые при разноске; проверь, что согласен:
  - [ ] **Кабинет v1 внесён в Exit Gate B** (порог живого гостя) и в Phase 1 как условие запуска; обязательный скоуп v1 (заселение с QR и кодом крупно, «Сотрудники», очередь заявок; mobile-first) — комментарий в #48, туда же влит #56.
  - [ ] **Чтение из Opera поднято из Phase 4 в Phase 1** (после кабинета): черновик ADR-004 — #121, `PmsPort` + Fake-адаптер — #122 (первый порт = канон интеграций); пункт «начало общения с Oracle по OHIP» из плана снят (OHIP — про OPERA Cloud, у пилота 5.6 on-prem).
  - [ ] **Онбординг пилотного тенанта — #123**: шесть категорий, черновик сроков напоминаний (reception/fnb 10, maintenance 15, housekeeping/it-support 30, laundry 60 мин), `reception_phone`.
  - [ ] **Бюджет LLM — #125**: предложено ~$10–15/день (потолок ≈ $450/мес — сверь с экономикой пилота); #103 (алерт 80%) поднят в milestone «Пилот: первый отель».
  - [ ] **Спека политик обслуживания — #124** (квоты + платные альтернативы, `guests_count` в `Stay`, цена в данных тенанта).
  - [ ] **Юрпакет упакован**: #126 (docs/legal/ + runbooks), #127 (consent-gate Telegram), #128 (маскирование карт), #42 (ретеншн 90 дней — уточнена комментарием); порядок за ADR-006 (#49).
- [ ] **Аудит плана и трекера (29.07, сессия Fable)** — действия, выполненные от твоего имени; спот-чекни:
  - [ ] **#49 переоткрыт**: PR #130 закрыл его случайно — GitHub прочёл «Не Closes #49» в теле PR как закрывающее слово (русское отрицание не парсится). Гейт переезда снова виден; DoD-чеклист — в ADR-006 §«Условия соответствия». Урок дописан: closes/fixes/resolves #N не писать даже в отрицании.
  - [ ] **Заведены #133–#138** — пункты Gate B, которые план обещал текстом, но которых не было в трекере: #133 outbox dead-letter + алерт (это Exit-критерий Gate B), #134 сетевой I/O вне транзакции, #135 бэкап перед `alembic upgrade`, #136 liveness воркера, #137 fail-fast `LLM_MODEL`, #138 prompt caching (развязан с Haiku-экономией: кэш не трогает качество, порог Sonnet пройден).
  - [ ] **Закрыты как сделанные/дубликаты: #37, #44, #56, #60, #61** — у каждого доказательство в закрывающем комментарии (код/тест/ADR-012). Не согласен — переоткрой.
  - [ ] **Майлстоун «Пилот: первый отель» пополнен: #46, #62, #101, #106** — были без майлстоуна, хотя по смыслу пилотные.
  - [ ] **PR #132 (consent-gate, spec 0029) смержен** после ревью уровня A отдельной сессией (APPROVE); рекомендации — хвост #139. Твоя проверка на staging: написать боту → должен прийти экран согласия с кнопкой; открыть `https://staging.necturn.com/legal/privacy`.

- [ ] **Частичное выполнение: живой текст гостю (29.07, spec 0030)** — по твоему багу с уборкой и сломанным пылесосом. Решения, принятые при реализации:
  - [ ] **Закрыт только вопрос 1 issue #58** («что видит гость»): текст о частично выполненной заявке собирает модель. Вопросы 2–4 (кто доделывает остаток, нужен ли статус «заблокирован», причина — список или текст) остались открытыми за тобой — это же Q9 операционного аудита ниже.
  - [ ] **Промпту запрещено что-либо обещать гостю** (срок, ремонт, «вернёмся»), если этого не сказал персонал: остаток работы сегодня не становится ничьей задачей, и обещание отель не исполнит. Как только остаток станет заявкой (вопрос 2), запрет можно снимать.
  - [ ] **Отмену с причиной не трогали** — «пришлось отменить» + «От персонала: …» само по себе не противоречиво. Скажи, если хочешь живой текст и там.
  - [ ] **Цена**: один вызов LLM на каждое частичное закрытие (не на каждое закрытие).

## Открытые решения, которые ждут именно тебя

- [ ] **Кто вправе приглашать сотрудников** (только manager/director или любая роль своей службы) — понадобится к кабинету v1 / RBAC-матрице (DISCUSSION_LOG «Доступ персонала»).
- [ ] **Нужна ли 2FA управляющим ролям на пилоте** — ADR-008 сознательно вынес за скоуп; решить до кабинета v1.
- [ ] **Формат статического ответа неавторизованным**: язык(и), per-tenant текст, телефон ресепшена (DISCUSSION_LOG «Неавторизованный трафик») — понадобится в спеке #79.
- [ ] **Юрпакет — решений больше не ждёт, ждёт работы** (сверено 27.07). Все четыре развилки ты уже закрыл, они в [PROJECT_EXECUTION_PLAN.md](../PROJECT_EXECUTION_PLAN.md) §Gate B: consent-gate вариант A (кнопка), ретеншн 90 дней, Kazteleport, ИП сейчас / ТОО при выходе за 2–3 отеля. Фактический статус работ: сделан только `PII_REGISTRY.md` и согласие v0 в веб-чате; **не сделано** — каталога `docs/legal/` не существует (нет политики конфиденциальности, текста согласия kk/ru/en, DPA-раздела), ADR-006 не написан, consent-gate в Telegram нет, ретеншн `messages` не реализован (нет даже настройки — переписка копится вечно), маскирование карточных номеров нет, runbooks `breach-response.md` и `subject-requests.md` отсутствуют. Из этого твоего участия требуют только: регистрация ИП, договор/аккаунт Kazteleport, приказ об ответственном лице, договор с отелем с DPA-разделом. Остальное — 1–2 дня работы LLM-сессий. **Упаковано в задачи 28.07:** #126 (доки + runbooks), #127 (consent-gate Telegram), #128 (маскирование карт), #42 (ретеншн — уточнена); порядок по §«Порядок и упаковка» рекомендаций: ADR-006 (#49) → #126 → код.
  - [ ] **Проверить с юристом срочность ИП.** В плане записано «ИП до первых денег», но обязанности оператора ПД возникают от самой обработки, а не от оплаты; плюс без юрлица не с кем подписывать договор с отелем, а без договора отель юридически не поручал тебе обработку данных своих гостей. Похоже, ИП нужен **до пилота**, даже если пилот бесплатный.
  - Напоминание: **локализация (ст. 12) блокирует продакшен уже сейчас**, а не с какой-то даты; 25.08.2026 — срок вступления отдельной нормы (реестры), не отсрочка для локализации.
- [ ] **Оставшиеся вопросы операционного аудита: Q9, Q10, Q11, Q13** (Q1–Q8 закрыты, см. DISCUSSION_LOG «Открытые вопросы операционного аудита»): частичное выполнение заявки; политика на оскорбления в чате; кто вправе на comp; **Q13 — дневной лимит LLM $5 упирается в объём 310-номерного отеля**, нужно поднимать под пилот + доделать алерт на 80% (#103). Заведено 28.07: #125 (лимит; цифру ~$10–15/день подтверди), #103 — в milestone «Пилот: первый отель».
- [ ] **Юристу вместе с политикой: достаточность consent-экрана v3** (29.07, по твоему требованию — spec 0029 §10). Экран теперь — одна строка: «Продолжая, вы соглашаетесь с обработкой персональных данных согласно Политике конфиденциальности: ссылка» + кнопка «Продолжить». В политику по ссылке перенесены: явное упоминание передачи в США (ст. 16), факт ИИ-обработки и право на сотрудника (ст. 19-1). Вопрос юристу: достаточно ли согласие кнопкой под такой строкой для ст. 8/16/19-1, когда всё это названо только в политике. Если нет — короткие упоминания возвращаются в экран (правка одного файла, версия v4). Кнопка сохранена: без явного действия согласие недоказуемо, и первое сообщение гостя ушло бы в обработку до согласия.
- [ ] **К 25.08.2026 — проверить пороги уведомительного реестра операторов ПД** (закон № 231-VIII) и подписаться на обновления подзаконки МИИЦР — юр-рекомендации §4 (`docs/audits/2026-07-22-legal-recommendations.md`). Единственный календарный юрпункт, не попавший в эту очередь раньше (найдено аудитом 29.07).
- [ ] **Подать заявку на WhatsApp BSP** — план Phase 1 называет это параллельным бизнес-процессом с долгим lead time (критерии выбора — FOUNDATION §17.4: обязателен веб-инбокс + отдельный выделенный номер под ИИ). Пилот не блокирует; блокирует канал WhatsApp в Phase 2 — чем позже заявка, тем позже канал.
- [ ] **Основной поток выдачи гостевого доступа** (DISCUSSION_LOG «Как гость получает доступ при 310 номерах») — ты сказал, что подумаешь. Развилка: код всем при заселении / подтверждение по запросу гостя / оба; печатный талон или ручка; цифровой код; перехват печати регкарты как план Б. Для окончательного ответа не хватает двух фактов у отеля: **доступен ли принтер стойки с рабочего компьютера как обычный** (печать из браузера) и **остаётся ли подписанная регкарта в отеле или гость уносит копию**.

## Технический долг до пилота (не забыть)

- [ ] **Bake-off concierge_v4 на живых моделях не прогнан** (промпт менялся в spec 0025, а выбор модели по ADR-010/012 обязан опираться на измерение) — нужен `ANTHROPIC_API_KEY` и ~час прогона.
- [ ] **Побочные issues последней серии**: #101 (инструмент `escalate_to_staff` + prompt v4), #103 (алерт ~80% дневного LLM-бюджета), #106 (алерт «бэкапа нет N часов») — 29.07 подняты в майлстоун «Пилот: первый отель»; #37 сверен и закрыт 29.07 (номер комнаты и перевод summary давно в коде).
- [ ] **Живой прогон staff-UX чек-листа на staging** (`docs/runbooks/staff-ux-check.md`) — не гонялся после серии 0022–0029 (эскалация, маршрутизация, «мои заявки», веб-чат, напоминания, consent-gate); сделать до старта кабинета v1 — кабинет строится поверх этих потоков.
- [ ] ~~**ADR-006 (хостинг/резидентность РК) не написан**~~ — написан 28.07 (PR #130); открытым остаётся переезд прод-контура — #49 (29.07 переоткрыт после случайного автозакрытия, см. блок аудита выше).
- [ ] **Трёх категорий пилота нет в системе** (27.07): в `tools/seed.py` заведены только `housekeeping`, `maintenance`, `it-support`; у пилотного отеля есть ещё **ресепшен, F&B и прачечная** — их заявкам сейчас приземляться некуда, модель разложит их по трём существующим. Категории — данные в БД, работа онбординга тенанта. Сроки напоминаний по категориям — черновик в DISCUSSION_LOG «Профиль пилотного отеля». **Заведено 28.07 → #123** (категории + сроки + `reception_phone`).
- [ ] **Ручное заселение при 100–150 заселениях в день** — узкое место запуска: пропущенное заселение проявляется у гостя («введите код», кода нет), а в логах всё зелёное. Отсюда два следствия: кабинет персонала v1 (#48) становится условием запуска, а не улучшением; источник проживаний из Opera (выгрузка) — следующая задача после кабинета, а не Phase 3. См. DISCUSSION_LOG «Как гость получает доступ при 310 номерах». **Разнесено 28.07:** #48 (скоуп v1 + условие запуска в Exit Gate B), ADR-004 — #121, `PmsPort` — #122; план обновлён (Gate B «Профиль пилотного отеля», Phase 1).
