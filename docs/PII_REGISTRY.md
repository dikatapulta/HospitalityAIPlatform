# Реестр персональных данных (PII) — FOUNDATION §9

Каждое поле с персональными данными: где живёт, зачем, срок хранения, как
маскируется в логах. **Обновляется в том же PR, что и миграция, добавляющая
PII** (§9); ревью миграции без строки здесь — блокер. Правовая база — юраудит
22.07.2026 (`docs/audits/2026-07-22-legal-recommendations.md`): Закон РК
«О персональных данных» (локализация — ст. 12), GDPR для иностранных гостей.

Сроки хранения, помеченные «юрпакет», — рекомендация юраудита (90 дней),
финальное решение — задача «Юрдокументы» Gate B; до неё данные не удаляются,
но механика удаления обязана быть возможной без разрушения агрегатов (§9).

## Поля

| Поле | Модуль / таблица | Зачем | Срок хранения | В логах |
| --- | --- | --- | --- | --- |
| `guests.display_name` | guests (0014) | Обращение к гостю, карточка заселения | до удаления по запросу; ретеншн — юрпакет | не пишется |
| `stays.room_number` | guests (0014) | Привязка кода, заявки, эскалации | как у Stay | пишется (без имени комната не идентифицирует человека сама по себе; связка имя+комната в логи не попадает) |
| `guest_sessions.consent_at/_version` | guests (0014) | Доказательство согласия на обработку ПД | как у сессии (доказательная запись) | не пишется |
| `messages.text` | channels (0008) | История диалога гостя; может содержать любые ПД, названные гостем | 90 дней — юрпакет (retention не реализован, задача Gate B #42) | не пишется (логируются только id и метаданные) |
| `conversations.external_id` | channels (0008) | Идентификатор чата гостя у провайдера (Telegram chat.id / web-клиент UUID) | как у диалога | пишется (технический идентификатор; сопоставление с человеком — только внутри платформы) |
| `service_requests.summary/details/room_number` | requests (0006) | Суть заявки гостя | анонимизация свободного текста через 90 дней — юрпакет (#42) | summary пишется в уведомлениях/логах штатно — это рабочий текст для персонала |
| `service_requests.guest_language` | requests (0012) | Язык уведомлений гостю | как у заявки | пишется (не идентифицирует человека) |

## Не-PII, но секреты (для полноты картины)

`stay_access_codes.code_hash` (bcrypt), `guest_sessions.token_hash` (SHA-256) —
секреты доступа, в открытом виде не хранятся и не логируются никогда
(ADR-008); показываются один раз в момент выдачи.

## Правила

1. Новая PII-колонка → строка в таблице выше в том же PR (§9).
2. PII в логи не пишется; техидентификаторы (chat_id, UUID) — допустимы.
3. Удаление данных гостя по запросу (GDPR/ЗПД РК) проектируется при
   добавлении поля: PII должна быть удаляемой без разрушения агрегатов.
