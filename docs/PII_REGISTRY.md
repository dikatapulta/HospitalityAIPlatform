# Реестр персональных данных (PII) — FOUNDATION §9

Каждое поле с персональными данными: где живёт, зачем, срок хранения, как
маскируется в логах. **Обновляется в том же PR, что и миграция, добавляющая
PII** (§9); ревью миграции без строки здесь — блокер. Правовая база — юраудит
22.07.2026 (`docs/audits/2026-07-22-legal-recommendations.md`): Закон РК
«О персональных данных» (локализация — ст. 12), GDPR для иностранных гостей.

Сроки хранения решены юрпакетом (issue #126, 28.07.2026) и **исполняются
автоматически** (issue #42, spec 0032): воркер удаляет тексты сообщений старше
90 дней (вместе с опустевшими давно не обновлявшимися диалогами) и обезличивает
свободный текст заявок того же возраста (`channels/common/retention.py`,
настройка `MESSAGES_RETENTION_DAYS`). Удаление по обращению субъекта раньше
срока — вручную (`docs/runbooks/subject-requests.md`). Публичные тексты — в
`docs/legal/` (политика, согласие `consent_version`).

## Поля

| Поле | Модуль / таблица | Зачем | Срок хранения | В логах |
| --- | --- | --- | --- | --- |
| `guests.display_name` | guests (0014) | Обращение к гостю, карточка заселения | до удаления по обращению субъекта (runbook subject-requests) | не пишется |
| `stays.room_number` | guests (0014) | Привязка кода, заявки, эскалации | как у Stay | пишется (без имени комната не идентифицирует человека сама по себе; связка имя+комната в логи не попадает) |
| `guest_sessions.consent_at/_version` | guests (0014) | Доказательство согласия на обработку ПД (веб: своё на каждую привязку) | как у сессии (доказательная запись) | не пишется |
| `conversations.consent_at/_version` | channels (0016) | Доказательство согласия на обработку ПД (telegram: consent-gate, spec 0029) | как у диалога (доказательная запись; перезапрос согласия перезаписывает пару — действует последнее). Диалог, опустевший и не обновлявшийся 90 дней, удаляется ретеншном (#42) — вернувшийся гость проходит consent-gate заново | версия пишется, момент — нет (версия не идентифицирует человека) |
| `messages.text` | channels (0008) | История диалога гостя; может содержать любые ПД, названные гостем. Платёжные паттерны (PAN, NG-3) маскируются ДО записи и до LLM: контракт нормализации каналов + `shared/pii.py` (issue #128, spec 0031) | 90 дней — строка удаляется джобой ретеншна (#42, spec 0032) | не пишется (логируются только id и метаданные) |
| `conversations.external_id` | channels (0008) | Идентификатор чата гостя у провайдера (Telegram chat.id / web-клиент UUID) | как у диалога (опустевший и не обновлявшийся 90 дней — удаляется ретеншном #42) | пишется (технический идентификатор; сопоставление с человеком — только внутри платформы) |
| `service_requests.summary/details` | requests (0006) | Суть заявки гостя | 90 дней — затем обезличивание джобой ретеншна (#42, spec 0032): summary → плейсхолдер, details → NULL; статус/категория/времена остаются для отчётов | summary пишется в уведомлениях/логах штатно — это рабочий текст для персонала |
| `service_requests.resolution_note` | requests (0021 П-4) | Примечание персонала к закрытию (частичное выполнение / причина отмены); может пересказывать слова гостя | 90 дней — затем NULL (та же джоба #42) | не пишется (уходит гостю в уведомлении, в логах только id) |
| `service_requests.room_number` | requests (0006) | Привязка заявки к комнате | не обезличивается: структурированное поле, само по себе человека не идентифицирует (связка «комната → человек» живёт в Stay со своим сроком), нужно отчётам | пишется (тот же довод, что у `stays.room_number`) |
| `service_requests.guest_language` | requests (0012) | Язык уведомлений гостю | как у заявки | пишется (не идентифицирует человека) |
| `service_requests.claimed_by_display_name` | requests (0018) | Снапшот имени сотрудника, взявшего заявку («взял: Имя» в кабинете, spec 0033 §5) | как у заявки: PII сотрудника, не гостя — ретеншном гостевых текстов (#42) не обезличивается, история «кто делал» атрибутируема; удаление по обращению субъекта — runbook subject-requests | не пишется (в логах — user_id) |
| `users.display_name` | platform (0017) | Имя сотрудника: подпись «кто взял» в заявках, список «Сотрудники» | пока учётка существует (деактивация статус меняет, запись остаётся — история действий атрибутируема); удаление по обращению субъекта — runbook subject-requests | не пишется (в логах — user_id) |
| `user_identities.external_id` | platform (0017) | Для `kind=password` — email сотрудника: логин кабинета (spec 0033 §3.1); НЕ канал доставки | как у учётки | не пишется никогда (правило 2; в ключ rate-limit уходит SHA-256 email) |
| `staff_invites.invited_name` | platform (0017) | Имя приглашаемого — становится `display_name` созданного User | как у инвайта (доказательная запись «кто кого пригласил») | не пишется |

## Не-PII, но секреты (для полноты картины)

`stay_access_codes.code_hash` (bcrypt), `guest_sessions.token_hash` (SHA-256),
`user_identities.secret_hash` (argon2id — пароль сотрудника),
`staff_sessions.token_hash` и `staff_invites.token_hash` (SHA-256, миграция
0017) — секреты доступа, в открытом виде не хранятся и не логируются никогда
(ADR-008); показываются один раз в момент выдачи (пароль не показывается
вовсе — его знает только сотрудник).

## Правила

1. Новая PII-колонка → строка в таблице выше в том же PR (§9).
2. PII в логи не пишется; техидентификаторы (chat_id, UUID) — допустимы.
3. Удаление данных гостя по запросу (GDPR/ЗПД РК) проектируется при
   добавлении поля: PII должна быть удаляемой без разрушения агрегатов.
