# QUEUE.md — очередь работ

> **Роль:** единственное место, где живёт **порядок** работ. Что именно делать — в issue;
> зачем и по каким критериям — в [PROJECT_EXECUTION_PLAN.md](../PROJECT_EXECUTION_PLAN.md);
> здесь — только «что раньше, что позже» и кто это берёт.
>
> **Статус:** действующий. Заведён 2026-08-04 (правило исполнения 10). Порядок в разделах
> 1–2 выведен из «Exit Gate B» и списка «Осознанно НЕ в Gate B» того же плана — это не
> свежая расстановка приоритетов, а перенос уже принятых решений в один упорядоченный
> список. Два пункта план прямо не называет (#43, #54) — их обоснование в самих строках.

## Как этим пользоваться

Нормативная механика — правило исполнения 10
[PROJECT_EXECUTION_PLAN.md](../PROJECT_EXECUTION_PLAN.md); здесь — операционная шпаргалка.

- **Начало сессии** — `make queue`: раздел 0 живьём из GitHub, верх раздела 1
  с пометками «занято», расхождения файла с GitHub.
- **Заявка на пункт** — первым действием сессии: ветка, пустой коммит, draft-PR
  с номером issue в заголовке «… (#N)» — по заголовку `make queue` пометит пункт занятым.
- **Конец сессии** — правка этого файла **в том же PR**, что и работа: вычеркнуть
  сделанное, вписать issue, которые породили ревью или аудит; плановую работу без
  номера issue — строкой с обоснованием. Находка, не попавшая ни в один раздел,
  для следующей сессии не существует. У ревью issue заводится не по умолчанию,
  а по одному из четырёх оснований правила 9 (плюс предложения улучшений и
  находки повторного прохода о нетронутой части diff) — дефект, не попавший
  ни в одно, чинится в том же PR.
  Умолчание места новой находки — **раздел 3**; в раздел 1 она встаёт только
  с обоснованием прямо в строке: Exit-критерий Gate B, данные гостя, деньги
  или необратимый ущерб (правило 9). Умолчание — про место новой строки, а не
  про приоритет: перестановка уже стоящих строк и подъём из раздела 3 остаются
  за основателем. Косметика и несрочные процессные правки своей строки здесь
  не получают — они копятся строками в issue процесс-батча **#275** (правило 11),
  а «не попало ни в один раздел» на них не распространяется: сам #275 стоит
  пунктом в разделе 3, и `make queue` печатает, созрела копилка или нет.
  Вписал строку в #275 — исполнителя искать не нужно: батч возьмёт сессия,
  увидевшая «созрел» (правило 11).
- **Разметка — контракт для `make queue`:** номер **жирным** (`**#N**`) — пункт очереди,
  обязан быть открытым issue; номер обычным текстом — упоминание в обосновании.
  Порядковых номеров у пунктов нет: порядок — это порядок строк, позицию печатает скрипт.

**Порядок меняет основатель** — перестановка строк здесь и есть решение о приоритете.
LLM-сессия переставляет строки только по явной просьбе; молча — никогда (R-11).

Модель и effort у каждого пункта — по правилу 7 плана; это стартовая оценка, а не приговор:
не решено за два захода — лестница эскалации там же.

---

## 0. Открытые PR — этот раздел ведёт GitHub, не файл

Раздел 0 — живой список открытых PR; его печатает `make queue` прямо из GitHub,
руками он не ведётся. У строки в файле не было бы владельца, который её вычеркнет
(ревьюер не пишет в чужую ветку, а мержащая сессия правит только свою строку), и она гнила бы
за минуты — «ждёт merge» пережил сам merge через 11 минут после коммита (ревью PR #176).
Что делать с каждым статусом — подписано в выводе `make queue`; приоритеты — правило 10.
Уровень ревью автор указывает в описании PR (правило 9) — смотреть там.

---

## 1. Дорожка «Код» — Gate B, по порядку

Порядок — по ущербу для пилота (правило плана), внутри равного ущерба — дешёвое раньше.

### Сначала: дешёвое, что мешает всему остальному

- ~~#172~~ — маскирование карт калечило UUID в командах персонала — закрыт PR #202
  (команда разбирается из сырого текста, примечание закрытия маскируется; spec 0031 §2).
- ~~#203~~ — число длиннее int32 в аргументе staff-команды роняло вебхук молча — закрыт тем же PR #202
  (четвёртый проход ревью: кусок карты уходил наружу той же числовой веткой, и чинится это не ещё одной формой, а потолком дневного номера — тем самым, который предписывала #203; заодно `isdecimal()` вместо `isdigit()`).
- ~~#206~~ — реплай-команда персонала закрывала чужую заявку (обратный поиск по message_id без учёта чата) — закрыт PR #242
  (условие по `conversation_id` реплая в `load_request_id_for_staff_message` + тест кросс-чатовой коллизии; других обратных поисков по `external_message_id` в коде нет).
- ~~#207~~ — логин кабинета: rate-limit по IP один на весь отель за туннелем и тратился успешными входами — закрыт PR #244
  (канон `shared/clientip.py`: `CF-Connecting-IP` от доверенного прокси, `TRUSTED_PROXY_IPS`; бюджет тратят только неудачи — `peek_rate_limit` + `record_failed_login`; отдельный, более просторный бюджет IP-ключа. Прослойка `--proxy-headers` у uvicorn включена по умолчанию и выключается явным `--no-proxy-headers` в команде запуска — иначе адрес сокета подменяем переменной `FORWARDED_ALLOW_IPS` — находка ревью).
- ~~#179~~ — чек-лист онбординга: новый тенант недостижим (`TELEGRAM_TENANT_SLUG`, первый менеджер кабинета) — закрыт PR #249
  (шаг «`TELEGRAM_TENANT_SLUG` в `.env` + рестарт» и шаг «первый менеджер `staff_bootstrap`» в рунбуке, проверки «заявка у нужного отеля» и «уведомление дошло до чата службы» в чек-листе приёмки; немой отказ «оба staff-адреса пусты» назван там же, ловушка smoke — #250).
- ~~#248~~ — навык `/verify` поднимал uvicorn без `--no-proxy-headers` — закрыт PR #253
  (флаг дописан, все три пути запуска ведут себя одинаково; заодно `.claude/skills/` выведен из `.gitignore` — иначе правка жила бы только на машине основателя, вне ревью и вне `main`).
- ~~#254~~ — конфигурация Claude Code жила вне репозитория: хук `make check` перед коммитом можно было выключить молча — закрыт тем же PR #253
  (смежная находка #248; решение основателя 09.08 — коммитить `settings.json`, личный `settings.local.json` остаётся за игнором. Хук исполняется у каждой сессии без подтверждения, поэтому его правка внесена в уровень A правила 9).

### Exit-критерий «уведомление службе не теряется молча»

- ~~#133~~ — outbox: dead-letter + алерт `event_delivery_exhausted` + retention исчерпанных — закрыт PR #256
  (`dead_lettered_at` как терминальное состояние строки вместо вычисления `attempts < max_attempts` над текущим конфигом; алерт ERR-EVENTS-002 шлёт воркер — только он знает момент похорон и несёт correlation id, которого нет у watchdog'а без БД; retention один на оба терминальных исхода; ADR-015. Заодно закрыт ~~#18~~ — обе его половины уже жили в ADR-009 и коде, оставалось имя `cleanup_processed_events`, ставшее ложью.)
- ~~#134~~ — outbox: сетевой I/O доставки вынести из блокирующей транзакции пачки — закрыт PR #259
  (три шага вместо одной транзакции на пачку: «прочитать пачку» → доставка вне транзакций → «зафиксировать исход» на каждое событие. Защиту от двойного взятия строки даёт теперь аренда `locked_until`, а не `FOR UPDATE` на всё время сетевого вызова; `SKIP LOCKED` остался на время самой отметки о взятии. ADR-016.)
- ~~#136~~ — алерт liveness воркера: мёртвый воркер = молчание всей доставки (sec M11) — закрыт PR #262
  (пульс воркера строкой в БД, а не в Redis: в Redis отметка теряется при рестарте, и «пульса нет» стало бы
  неотличимо от «воркер ни разу не стартовал» — тот самый случай, ради которого алерт заводился. Сравнивает
  возраст с порогом и алертит алертер снаружи — по метрике `worker_heartbeat_age_seconds` из `/metrics`,
  доступа к БД у него нет и не должно быть (ERR-OPS-003). Вторая линия на тот же симптом — порог глубины
  `outbox_pending_events` (ERR-OPS-004): она ловит живой цикл, который очередь всё равно не разбирает.)

### Exit-критерий «бюджет и лимиты держат»

- ~~#125 + #103~~ — поднять дневной лимит LLM под масштаб отеля + алерт на ~80% — закрыты PR #265
  (лимит остался платформенным и поднят $5 → **$10/день**: пер-тенантное поле дало бы настройку,
  которую на пилоте некому различать, а алертеру пришлось бы знать лимит каждого отеля отдельно —
  минимальное достаточное, P-1. Цифра — оценка расхода ×2 с запасом, подтверждение за основателем
  ([FOUNDER_REVIEW_QUEUE.md](FOUNDER_REVIEW_QUEUE.md)). Алерт — ERR-OPS-006 по канону #136:
  пара метрик `llm_daily_spend_usd` / `llm_daily_budget_usd` в `/metrics` → порог
  `ALERT_LLM_BUDGET_RATIO` → ветка машины состояний алертера, состояние своё на каждого тенанта.
  Снимок считает владелец данных — `ai/gateway`, потому что `llm_call_log` лежит под RLS и
  платформенная сессия его не видит; kernel зовёт его через реестр обновляторов, подключаемый
  composition root'ом.)
- ~~#137~~ — fail-fast валидация `LLM_MODEL` против прайс-листа на старте (sec H5) — закрыт PR #266
  (`settings.llm_model` сверяется с ключами `MODEL_PRICING_USD_PER_MTOK` в обоих composition root'ах —
  `app.py` и `worker.py`; модель вне прайс-листа → `SystemExit` с текстом «исправьте .env» по канону
  конфигурационного отказа `shared/alerting.py`, процесс не поднимается. Прежде неизвестный id вскрывался
  только в `_compute_cost` — уже после оплаченного ответа провайдера: 500 первому же гостю, строки в
  `llm_call_log` нет, и дневной бюджет слеп ровно на ошибочных вызовах.)
- ~~#46~~ — gateway: пауза между ретраями (backoff) и атомарная квота бюджета (TOCTOU) — закрыт PR #270
  (Плановый пакет «Security-1» — #41 + #46 + H5 — закрыт целиком. Резерв бюджета строкой с арендой
  в своей тенантной таблице, а не счётчиком в Redis: канон rate-limit fail-open ИМЕННО потому, что
  потолком расхода служит дневной бюджет, — положить бюджет в Redis значило бы ослепить оба щита
  одним отказом. Атомарность без нового примитива: резерв ставится ДО проверки, и обоим
  параллельным вызовам видны оба резерва. Пауза — формула ADR-009, потолок — существующий
  `LLM_TIMEOUT_SECONDS`, разброс добавлен по разделу «Пересмотреть» самого ADR-009: у шлюза
  вызывающих столько, сколько гостей пишет сейчас, и авария провайдера синхронизирует их
  таймауты — ровно тот случай, для которого ADR-009 джиттер и предусмотрел. ADR-017.)
- ~~#267~~ — fail-fast конфигурации не роняет процесс под супервизорами uvicorn — закрыт PR #282
  (Выбран вариант 2 развилки — проверки вынесены в ENTRYPOINT образа (`ops/entrypoint.sh` →
  `python -m hospitality.preflight` → `exec` команды), а `--workers 2` в CMD остался: вариант 1 чинил
  один симптом, вариант 2 закрывает класс — любая будущая fail-fast проверка старта попадает в
  `preflight.py` и получает то же поведение, а ENTRYPOINT переживает `command:` в compose, который
  переопределяет только CMD. Обоснование выбора — [FOUNDER_REVIEW_QUEUE.md](FOUNDER_REVIEW_QUEUE.md).
  Замер: до правки `--workers 2` с битым `LLM_MODEL` не выходил за 25 с (8 перезапусков детей),
  `--reload` висел живым; после — обе цели образа выходят с кодом 1 за ~1,2 с. Тем же PR закрыта
  вторая половина #273.)

### Exit-критерий «прод с бэкапами и отрепетированным restore»

- ~~#135~~ — бэкап БД перед `alembic upgrade` в `deploy.sh` (sec M10) — закрыт PR #284
  (снимает его сам канон `ops/backup/backup.sh`, получивший метку имени первым аргументом:
  второй экземпляр шифрования и retention разошёлся бы с оригиналом при первой правке (P-12).
  Retention стал свойством каталога, а не прогона — иначе ночной cron не убирал бы дампы
  деплоя, а деплой не убирал бы ночные. Снимок намеренно **не** считается бэкапом для
  `fetch.sh` и будущего алерта #106 — иначе умерший cron маскировался бы снимками деплоя.
  Провал снимка = стоп деплоя до миграции; аварийный рычаг `SKIP_PRE_MIGRATE_BACKUP=1` —
  для человека у консоли, в CI его нет.)
- ~~#106~~ — алерт «бэкапа нет N часов» — закрыт PR #287 (шестая линия алертера, **ERR-OPS-008**:
  каталог бэкапов хоста смонтирован в контейнер алертера только для чтения, и нет ни одного
  `hospitality-*.dump.age` свежее `ALERT_BACKUP_MAX_AGE_HOURS` (30 ч) → алерт в канал команды.
  Развилка из тела issue решена в пользу проверки в алертере, а не в `backup.sh`: чек внутри
  скрипта не видит случая «cron не запустился вовсе» — ровно той тишины, ради которой issue
  заведена. Снимки деплоя `pre-migrate-*` за бэкап не считаются (решение PR #284); нечитаемый
  каталог алертит наравне с отсутствующим бэкапом — «не знаю» здесь тоже отказ.)

### Аудит 05.08 — пилотный минимум (решения основателя, [файл аудита](audits/2026-08-05-operational-audit.md) §12)

Утверждено основателем 06.08 как условие запуска пилота (порог назван и у владельца —
«Exit Gate B» [плана](../PROJECT_EXECUTION_PLAN.md); состав и числа живут в §12 п. 1 файла аудита).

Ещё две вещи того же минимума уже стояли в очереди своими строками и **сюда не переставлены** —
перестановка есть решение о приоритете, а оно за основателем (правило 10): #101 (обещание
сотрудника свободным текстом — ниже, группа «Качество и полнота проверок»; аудит просит
пересмотреть место, находка Е-4) и #124 (спека политик обслуживания — ниже, группа «Масштаб
пилота»). Сегодня обе стоят ниже всей группы «важно до пилота» — предложение пересмотреть это
вынесено в [FOUNDER_REVIEW_QUEUE.md](FOUNDER_REVIEW_QUEUE.md).

- ~~#208~~ — ЧП и жалобы: статический перехват без LLM + признак срочности заявки — закрыт PR #291
  (две половины, spec 0034. Перехват стоит ПЕРВЫМ действием хода гостя — раньше rate-limit и раньше
  модели: лимит защищает бюджет LLM, а этот путь модель не зовёт вовсе, и отвечать «подождите пару
  минут» гостю, у которого горит номер, нечем оправдать. Язык ответа выбирает совпавший маркер
  («пожар» → ru, «fire» → en, «өрт» → kk) — языка гостя без LLM система не знает, а язык слова знает.
  Признак срочности — колонка заявки, а не статус и не категория: гейт подтверждения P-9 он снимает
  через ADR-018 (класс инструмента при этом не меняется — снятие спрашивается отдельно), персоналу
  виден маркером 🚨 в чате и в кабинете, а ночную доставку по нему ветвит #212. Заявку сам перехват
  НЕ создаёт — категории «ЧП» у тенанта нет, и какая служба ею владеет, решает основатель: #292.
  Текст гостю правил основатель в чате 19.08: номера экстренных служб из него убраны — внешние
  службы вызывает ресепшен, а не гость, и «звоните 112» на шумных соседей или кражу прямо неверно;
  остаётся телефон ресепшена из конфига тенанта. Тем же разговором сужен список маркеров: «спасите»
  и голое «врач» ловили «спасите, не работает кондиционер» и «есть ли в отеле врач?», а прямая
  просьба «вызовите полицию/охрану» не ловилась вовсе.)
- ~~#209~~ — ответ гостю в Telegram теряется молча при сбое Bot API — закрыт PR #295
  (развилка issue решена в пользу outbox — тем же доводом, которым spec 0022 §2 выбрала событие
  для эскалации: сбой отправки обязан переживаться ретраем, а не записью в лог. Реплика, не ушедшая
  с первой попытки, коммитится событием `telegram_reply.undelivered` и дожимается воркером — первая
  повторная попытка через 2 с, дальше экспонента ADR-009; в срок годности укладывается 9 попыток.
  Событие названо по каналу, а не по адресату: через него идут и ответ гостю, и ответ персоналу в
  чат службы. Возражение «реплика привязана к ходу и через минуту неактуальна» снято сроком
  годности: старше 10 минут не отправляется вовсе, вместо неё ERROR ERR-TELEGRAM-006. Путей
  окончательной потери ровно два — не встала в очередь и протухла в ней, оба громкие тем же кодом;
  третьего, dead-letter, при штатных настройках не бывает: десятая попытка приходится уже за
  границей срока годности, и подписчик там возвращается, а не бросает. Запись в историю оставлена
  прежней и усилена: исходящий `Message` пишется только после ФАКТИЧЕСКОЙ отправки, поэтому история
  остаётся тем, что гость видел, а недошедшая реплика в неё не попадает никогда. Настройки не
  заведено намеренно — срок годности живёт константой модуля, чтобы не плодить пятую копию числа
  по .env.example и двум compose-файлам.)
- ~~#210~~ — спека измеримости пилота — закрыта PR #297
  ([spec 0035](specs/0035-pilot-measurability.md): метки времени и «кто закрыл», источник заявки `origin`,
  форма ручного приёма, сводка дня простыми счётчиками. Главное решение спеки — асимметрия «когда» и «кто»:
  метки времени пишутся на каждом переходе независимо от канала, имена — только там, где личность есть,
  иначе медиана времени взятия молча описывала бы один кабинет, а Telegram-путь персонала выпал бы из
  измерения. Второе — каждое число сводки считает владелец своих данных: отдельного модуля отчётов не
  заводится, `analytics/` остаётся в Phase 4.)
- ~~#302~~ — CI красный у всех PR: `anthropic 1.0.0` тянет `httpx2` — закрыт PR #303 21.08
  (быстрой половиной: верхняя граница `anthropic<1`. Строка стояла первой не по трём обычным основаниям
  подъёма — их тут не было вовсе, — а по правилу 11: поломка самого конвейера (класс #240) «чинится
  немедленно отдельным PR». `check` обязателен при `enforce_admins: true`, поэтому красный CI держал
  merge всего репозитория. Локальный `make check` поломку не видел: в `.venv` стоял `anthropic 0.116.0`,
  а CI собирает окружение с нуля и берёт свежий мажор. Остаток того же класса — в разделе 3: переезд
  на SDK 1.x (#304) и верхние границы либо лок-файл для остальных зависимостей (#305).)
- ~~#298~~ — измеримость, шаг 1: метки времени, «кто закрыл», источник заявки `origin` — закрыт PR #309
  (миграция `0025` с бэкфиллом: `origin='guest_chat'` всем существующим строкам, `closed_at=updated_at`
  терминальным **кроме обезличенных** — у тех `updated_at` показывает прогон ретеншна, а не закрытие;
  `server_default` снят сразу после бэкфилла, поэтому INSERT без источника падает громко.
  `claimed_at` не бэкфиллится ничем намеренно: приблизительное значение попало бы прямо в метрику.
  Асимметрия §3 реализована так, как её описывала спека: метки времени пишет каждый переход из любого
  канала, имена — только переход с `acting_user`. `list_requests_closed_since` перешла на `closed_at`,
  и вместе с этим ушло условие по плейсхолдеру обезличивания — с `closed_at` древняя заявка не всплывает
  во вкладке по устройству, а не благодаря отдельной заплате. Смоук-сценарий §13 спеки в PR не вошёл —
  таблица §14 не приписывает его ни одному PR серии, он заведён отдельно: #310, раздел 3.)
- **#299** — измеримость, шаг 2: форма «Новая заявка» в кабинете — **Opus 5 `high`**
  Знаменатель того же критерия: заявок мимо бота в системе не существует, вносить их некуда (аудит А-5). Требует #298.
- **#300** — измеримость, шаг 3: числа сводки дня + страница в кабинете — **Opus 5 `xhigh`**
  Требует #298. Новая тенантная таблица эскалаций и миграция → уровень ревью A.
  До реализации §6/§9 снять расхождение знаменателя доли — #313 (раздел 3): §6 и §9 считают два значения `origin`, §4 — три, и без этого двузначная арифметика уедет прямо в плитку кабинета и в текст сводки.
- **#301** — измеримость, шаг 4: утреннее сообщение со сводкой + копия основателю — **Opus 5 `high`**
  Требует #300. Самый лёгкий из четырёх и единственный, который основатель может отложить без ущерба измерению.
- **#211** — спека: знания об отеле v0 + справочник в кабинете — **Opus 5 `high`**
  Факты в промпт под кэш, не инструментом; справочник правит менеджер (решение §12 п. 3). Полный RAG — после, когда факты исчерпаются.

### Аудит 05.08 — важно до пилота (порядок внутри группы — предложение аудита)

- **#213** — действия из кабинета не видны в Telegram-группах: подписчик на initiator=STAFF — **Opus 5 `high`**
- **#216** — заявку нельзя передать: смена службы + возврат `in_progress → new` — **Opus 5 `xhigh`**
- **#215** — подпись к фото гостя теряется целиком: `caption` читать как `text` — **Opus 5 `high`**
- **#225** — просроченные проживания: автозавершение по сроку + видимость в кабинете — **Opus 5 `xhigh`**
- **#226** — генератор комнатных QR на 310 номеров + шаг рунбука онбординга — **Opus 5 `high`**
- **#221** — очередь кабинета: поиск, история за 7 дней, фильтр в SQL, поллинг не затирает форму — **Opus 5 `high`**
- **#224** — казахский во всех статических текстах гостя (решение §12 п. 6) — **Opus 5 `high`**
- **#223** — веб-гость не получает «выполнено/отменено» после закрытия вкладки — **Opus 5 `high`**
- **#222** — веб-чат: тишина после превышения лимита — нужен статус «пауза» — **Opus 5 `high`**
- **#271** — при деградации провайдера гость ждёт около полутора минут до «не могу ответить, зову сотрудника» — **Opus 5 `xhigh`**
  Место выбрано рядом с #222 не по тяжести, а по классу: из кресла гостя это один и тот же отказ — экран молчит, признака жизни нет, непонятно, ждать или идти на ресепшн. Держать один случай тишины в блокирующем разделе, а второй в отложенном значило бы разделить один продуктовый вопрос между двумя горизонтами. Развилку (общий дедлайн хода / меньше попыток / асинхронный ход) сессия решает сама — черновик в самой issue, — но **вариант «асинхронный ход» в Gate B не входит**: он меняет контракт каналов и требует ADR, это Phase 1. Ожидаемое поведение гостя показать основателю абзацем ДО реализации (CLAUDE.md). Уровень A правила 9 — трогается канон обращения к LLM. Найдено при #46, основание (б) — PR #270 трогает паузы ВНУТРИ этого ожидания, а не само ожидание; расстановку основатель делегировал сессии-ревьюеру 13.08 ([FOUNDER_REVIEW_QUEUE](FOUNDER_REVIEW_QUEUE.md)).
- **#220** — кабинет: отзыв сессий при потерянном телефоне — **Opus 5 `high`**
- **#214 + #217 + #218** — мини-фиксы Telegram одной сессией: ForceReply `selective`, `details` в уведомлении, заглушка лички сотрудника — **Opus 5 `high`**
- **#243** — апгрейд staff-группы до супергруппы меняет `chat.id`: уведомления не доходят, а сама группа становится «гостевой» — **Opus 5 `high`**
  Найдена ревью PR #242, основание (б) правила 9 (тот PR этого не трогал). Строку вписала сессия #207: `make queue` показал issue вне разделов, а не попавшая ни в один раздел находка для следующей сессии не существует. Место выбрано рядом с прочими телеграм-фиксами того же класса (#218 — тот же исход «персонал принят за гостя»); порядок — за основателем (правило 10).
- **#229 + #230** — надзор за алертером (Sentry + heartbeat) и ротация docker-логов — **Opus 5 `high`**
- **#231** — закрыть публичные Swagger и `/metrics` — **Opus 5 `high`**
- **#232** — required checks ветки: `migrations` и `dev-environment` (исполнитель — по развилке #195) — **Opus 5 `high`**
- **#227** — `guests_count`: «3+» и CLI собирают неверные данные под квоты #124 — **Opus 5 `high`**
- **#228** — связать заявку со Stay: фундамент квот #124 и сводки #210 — **Opus 5 `xhigh`**
- **#212** — часы работы служб и ночной гибрид (решение §12 п. 2; после #208 — общий признак срочности) — **Opus 5 `xhigh`**
- **#219** — офбординг: состав Telegram-групп служб в рунбук + сверка — **Opus 5 `high`**
- **#233** — рунбуки отстали: secrets.md (креды туннеля), deploy.md, ufw в bootstrap — **Opus 5 `high`**
- **#251** — каталог ошибок: две диагностические команды `docker compose exec` без `-f` не запустятся на сервере — **Opus 5 `high`**
  Найдена при доработке PR #249 по той же находке ревью, основание (б) правила 9 (тот PR `errors.md` не трогал). Обе стоят в разделе «Что проверить» и выполняются в момент разбора инцидента — там лишний тупик дороже всего. Строку вписала сессия #179 рядом с #233 как тот же класс «рунбук отстал от реальности сервера»; место — за основателем (правило 10).
- **#252** — каталог ошибок: диагностика «из тенантной сессии» даёт неверное число — у запросов нет фильтра тенанта — **Opus 5 `high`**
  Найдена ревью PR #249, основание (б) правила 9 (тот PR `errors.md` не трогал). Тот же файл и тот же класс, что #251, поэтому строка стоит рядом: обе статьи каталога отвечают на вопрос «сколько потрачено / какие категории» и обе отвечают неверно — суперпользователем считается расход ВСЕХ тенантов, ролью приложения без GUC — ноль. Актуально с первого живого онбординга: чек-лист приёмки (#179) отправляет оператора за суточным расходом именно сюда, а алерта на 80% нет (#103). Строку вписала сессия #248 (`make queue` показал issue вне разделов); место в порядке работ — за основателем (правило 10).
- **#234** — «продлить выезд» = категория в reception без денег (решение §12 п. 4); v2 по занятости — после #122 — **Opus 5 `high`**

### Масштаб пилота: 100–150 заселений в день

- **#121** — ADR-004 (черновик): источник проживаний из Opera PMS 5.6 on-prem — **Fable 5 `xhigh`**
  Развилка способа чтения не решена (выгрузка отчёта / OWS / чтение БД / план Б) — зона Fable по правилу 7. На Fable уровень effort выставлять руками: переход сбрасывает его на `high`. **Состав выгрузки решением основателя 06.08 уже сужен** ([файл аудита](audits/2026-08-05-operational-audit.md) §12 п. 4): номер брони, занятость номеров, даты — имена гостей не выгружаются; ADR пишется под этот вход, а не выбирает его заново.
- **#122** — `PmsPort`: первый порт проекта + обязательный Fake-адаптер одним PR — **Opus 5 `xhigh`**
  Задаёт канон всех будущих интеграций (ADR-007), поэтому `xhigh`.
- **#124** — спека: политики обслуживания тенанта, квоты бесплатного и платные заказы — **Opus 5 `high`**
  **Входит в пилотный минимум** ([файл аудита](audits/2026-08-05-operational-audit.md) §12 п. 1 — «темп на #124»); строка оставлена на прежнем месте — переставляет основатель (правило 10). Пререквизит #79 закрыт. Нетривиальная фича начинается со спеки, а не с кода.

### Комплаенс и приватность до живых данных граждан РК

- **#161** — согласие сотрудника на ПД: показывается гостевой текст и гостевая политика — **Opus 5 `high`**
  Требует решения основателя (черновик — в самой issue).
- **#239** — канон маскирования карт обходится одной соседней цифрой: «4111 1111 1111 1111 0327» → полный PAN в `messages` на 90 дней, в контекст LLM и в staff-чат — **Opus 5 `xhigh`**
  Найдена ревью PR #202 (четвёртый проход), дефект не его: `shared/pii.py` вне diff, ведёт себя одинаково до и после merge (основания (б) и (в) правила 9). Щит NG-3 обходится не хитрым вводом, а обычным — гость пишет номер и срок через пробел. Правка идёт спекой 0031: там живёт правило кандидата и баланс «ложные срабатывания против пропусков», а он от неё смещается. Позицию строки выбрала сессия-автор доработки #202 — рядом с #13, перед ним: сначала чинится щит, который уже стоит, потом маскирование расширяется на логи; порядок за основателем.
- **#13** — автоматическое маскирование PII в логах (§10.1) — **Opus 5 `xhigh`**
  Пилот = реальные ПД граждан РК; сейчас риск закрыт только точечно.
- **#149** — retention для `staff_sessions` и `staff_invites` (FOUNDATION §9) — **Opus 5 `high`**

### Поверхности безопасности

- **#182 + #183** — Security-3: мета-тест RLS по `pg_class.relrowsecurity` (M7) + сузить гранты роли приложения на `tenants` (M2) — **Opus 5 `xhigh`**
  Тенант-изоляция (P-4, ADR-003), названы одной строкой плана — поэтому одной задачей. Половина планового пакета «Security-3 до первого гостя» (дорожка «Инфра/Legal», п. 8): план положил её к основателю, но это работа LLM-сессии; потеряна при заведении очереди, возвращена ревью PR #176.
- **#156** — код заселения: перевести rate-limit ввода в fail-closed (последствие Ф-2) — **Opus 5 `xhigh`**
- **#246** — у входа с доказанным паролем не осталось потолка: argon2 без лимита после #207 — **Opus 5 `xhigh`**
  Побочный эффект правильного фикса #207: бюджет тратят только неудачи, поэтому успешный вход и отказ деактивированному (ERR-AUTH-005) не стоят ничего. Проверка пароля — argon2id `m=64 MiB, t=3, p=4`, воркер на staging один: уволенный сотрудник с ещё живым паролем кладёт кабинет циклом. Кабинет — условие запуска пилота, актор бытовой (офбординг, ср. #219). Нужен новый потолок, число в spec 0033 §3.3 и тесты — поэтому issue, а не правка в PR #244 (основание (в) правила 9).
- **#245** — лимит по IP не связывает клиента IPv6: ключ — точный адрес, а у клиента целая /64 — **Opus 5 `xhigh`**
  Сменил адрес внутри своей подсети — счёт с нуля; `staff_login_ip` и `guest_bind_link_consume` перестают ограничивать перебор по учёткам (подбор пароля к одной учётке держит email-ключ). Регрессией не является: до #207 IP-ключ не работал вообще. Стоит сразу за #246 намеренно — тот же слой (`shared/clientip.py`, `shared/ratelimit.py`, spec 0033 §3.3) и те же тесты.
- **#174** — CSRF-щит форм: добавить `Sec-Fetch-Site` вторым сигналом — **Opus 5 `xhigh`**
- **#247** — CSRF сервисного API: сравнение `Origin` без схемы привязано к `--proxy-headers`, которого не будет, и ссылается на чужой #149 — **Opus 5 `high`**
  `staff_portal/api_router.py` планирует ужесточение «вместе с фиксом #149», но #149 — это ретеншн `staff_sessions`/`staff_invites`, а сам план отменён: после PR #244 прослойка выключена явно, за туннелем схема навсегда `http`, и триггер не наступит. Стоит сразу за #174 намеренно — тот же щит и тот же блок кода: если PR по #174 переписывает этот комментарий, дефект становится его собственным и чинится там же (умолчание правила 9), и #247 закрывается без отдельной сессии.
- **#158** — `Referrer-Policy: no-referrer` на bind-странице гостя (`channels/web`) — **Opus 5 `high`**
- **#157** — разрыв ошибочной привязки гостя: QR сфотографировали со стойки — разорвать нечем, кроме правки БД — **Opus 5 `xhigh`**
  Перенесён из раздела 3 решением основателя (04.08): сам issue («Почему до пилота») противоречил ярлыку «отложено». `reissue_access_code` сессий не гасит, `check_out` выселяет настоящего гостя, а инцидент не выглядит инцидентом — настоящий гость видит лишь «ссылка устарела».

### Дыры пути заявки, оставшиеся с ревью Фазы 0

- **#43** — идемпотентность фиксирует «получено», а не «обработано»: сбой хода теряет ответ гостю навсегда — **Opus 5 `xhigh`**
  Планом прямо не назван; в Gate B потому, что закрывается вариантом (б) — деградация + эскалация #36, то есть это остаток Exit-критерия «эскалация реально зовёт персонал».
- **#54** — отмена заявки: гость не узнаёт об отмене, причина не спрашивается — **Opus 5 `high`**
  Требует решения основателя ДО реализации: причина отмены — список или свободный текст (вопрос общий с #58, вынесен в FOUNDER_REVIEW_QUEUE). Планом прямо не назван; в Gate B потому, что это «обещание без эффекта» того же класса, что #36 — «#1 баг доверия» по плану.
- **#120** — заявка «взяли и не сделали»: напоминание о висящих `in_progress` — **Opus 5 `high`**
  Решение основателя от 27.07 (пересмотр spec 0028), механизм по умолчанию выключен.
### Качество и полнота проверок

- **#62** — bake-off меряет один ход и не имеет ассертов — **Opus 5 `high`**
  План требовал его до кабинета; кабинет сделан, долг остался.
- **#101** — модель обещает сотрудника свободным текстом (`TurnKind.REPLY`): эскалация не публикуется — **Opus 5 `high`**
  **Входит в пилотный минимум** ([файл аудита](audits/2026-08-05-operational-audit.md) §12 п. 1); строка оставлена на прежнем месте — переставляет основатель (правило 10), аудит просит место пересмотреть (Е-4). Перенесён из раздела 3 решением основателя (04.08): решение в issue уже принято (инструмент `escalate_to_staff`, spec 0022 переиспользуется целиком), ярлык «требует решения» не подходил. Третий и последний путь «обещания без эффекта» — остаток Exit-критерия «эскалация реально зовёт персонал»; частота меряется bake-off'ом #62, поэтому стоит сразу за ним.
- **#45** — CI: обещанная R-9 проверка «PR модуля трогает README» — **Opus 5 `high`**
  Аудит зависимостей из той же issue закрывает открытый PR #152 — в его заголовке номера нет, поэтому «занято» этого не покажет (осознанный размен ревью PR #176); остаётся R-9-джоба.
- **#255** — инвариант «`--no-proxy-headers` во всех командах запуска» держится только глазами ревьюера — **Opus 5 `high`**
  Найдена ревью PR #253; это предложение улучшения, а не дефект (сегодня флаг стоит во всех четырёх командах — проверено тем же ревью), поэтому issue, а не правка тем же PR (правило 9). Строку вписала сессия #248 рядом с #45 — тот же класс «правило записано, а машина его не проверяет», и та же цена: #248 прожил незамеченным ровно потому, что четвёртый путь запуска лежал в скрытом каталоге, куда не заходят ни `rg` по умолчанию, ни `git grep`. Место в порядке работ — за основателем (правило 10).
- **#139** — хвост ревью spec 0029: rate-limit на `/start` после согласия, тосты на kk, assert дословности — **Opus 5 `high`**
- **#169** — чипсы-радио на заселении без `:has()` — **Opus 5 `high`**
  Живая проверка кабинета с телефона из той же issue — раздел 2, это не работа LLM.

### Перед первым живым онбордингом отеля

Хвосты ревью PR #170 (04.08); #179 из этой группы поднят наверх решением основателя.
Команда `tools/onboard_tenant` живьём ещё не запускалась — всё это проявится при первом
запуске на настоящем отеле, а не в CI.

- **#178** — онбординг: вход оператора не проверяется до записи в БД (телефон, формат slug, дубли `key`, пустые `hints`) — **Opus 5 `high`**
  Формат slug важнее прочего: он уходит в QR, который печатается на 310 номеров, а переименования тенанта в репозитории нет.
- **#177** — повторный онбординг затирает настройки, сделанные `request_reminders` и будущими командами — **Opus 5 `high`**
  Плюс тест-перепись полей `TenantConfig`, чтобы следующее поле конфига (бюджет LLM, #125) не начало сбрасываться молча.
- **#180** — частично заполненные `category_hints` смещают выбор службы моделью — **Opus 5 `high`**
  Дешёвый вариант (назвать в отчёте службы без подсказки) закрывается сразу; точный меняет то, что видит модель, и меряется вместе с #62.
- **#250** — smoke бьёт мимо тенанта: гость идёт в `TELEGRAM_TENANT_SLUG`, проверка — в `SERVICE_TOKEN_TENANT_SLUG` — **Opus 5 `high`**
  Найдена при работе над #179, основание (б) правила 9 (тот PR smoke-набора не трогал). Проявляется ровно после шага 3 рунбука: пока оба slug'а — `demo-hotel`, всё сходится, а на инсталляции с живым отелем smoke краснеет и, что хуже, шлёт настоящему персоналу заявки-пустышки. Строку вписала сессия #179 в эту группу — все её пункты о том, что вскроется на первом живом онбординге; место — за основателем (правило 10).

### Выход из Gate B

- **#185** — промпт аудита: у аутентификации персонала в кабинете нет своей оси — **Opus 5 `high`**
  Не приоритет выше #151, а его пререквизит: прогон начинается с копии этого промпта, и ось, которой в промпте нет, аудитор вправе не смотреть. Найдено ревью PR #152.
- **#151** — аудит безопасности по коду Gate B: IDOR/object-level authz, приватность, треть кода без аудита — **Fable 5 `xhigh`**
  Сквозной аудит кодовой базы одним прогоном — зона Fable (правило 7).
- **Аудит Gate B** (правило 8) — **Fable 5 `xhigh`**, допустим разовый `max`
  Отдельная сессия с чистым контекстом: проверка Exit Gate B, соответствия конституции, честности документации. Issue заводится, когда все прочие пункты раздела 1 закрыты.

---

## 2. Дорожка «Инфра / Legal» — за основателем

Не конкурирует с дорожкой «Код» за одни руки: это шаги, которые LLM-сессия выполнить не может.

**Дедлайна у дорожки больше нет.** Стоявшее здесь «25.08.2026 — пороги реестра
операторов ПД» снято сверкой по первоисточникам 12.08.2026
([справка](audits/2026-08-12-pd-operator-registry-note.md)): уведомлять уполномоченный орган
обязаны только крупные операторы (от 500 000 уникальных субъектов), мы малый — к этой дате
не требуется ничего; сама дата принадлежит закону № 326-VIII от 24.06.2026, а не 231-VIII
(тот в силе с 18.01.2026). Настоящий гейт бессрочный и уже наступил: локализация базы в РК
(ст. 12) блокирует первого живого гостя — это #49. Триггеры вернуть вопрос реестра в работу —
в справке и в «Пересмотреть, когда» [ADR-006](adr/ADR-006-hosting-data-residency-kz.md);
ближайший достижим на пилоте (10 000 уникальных гостей — вопрос месяцев при 100–150 заселениях
в сутки).

**Готовый пакет по всем шагам ниже** — [runbooks/founder-actions-pilot-launch.md](runbooks/founder-actions-pilot-launch.md):
план по дням, чек-лист регистрации ИП, конфигурация и цена хостинга РК, два готовых письма
(IT-отделу отеля и юристу). Строки очереди остаются местом порядка, рунбук — местом шагов.

- **#49** — гейт продакшена: переезд в РК (Kazteleport) по DoD-чеклисту ADR-006 + выделенная non-superuser LOGIN-роль Postgres. **Блокирует первого живого гостя.**
- **Живой прогон staff-UX чек-листа на staging** — [runbooks/staff-ux-check.md](runbooks/staff-ux-check.md): ~20 минут с телефоном, путь персонала (сценарии С-1…С-13) руками; после серии 0022–0029 не гонялся. Долг назван статус-абзацем Gate B в плане; потерян при заведении очереди, возвращён ревью PR #176. Прошлый прогон (17.07) нашёл пять дефектов при зелёном CI.
- **#169** (вторая половина) — живая проверка кабинета персонала с телефона: пройти сценарий пальцем на реальном экране. Автотесты этого не видят.
- **Внешний uptime-монитор** на `https://staging.necturn.com/health/ready` (healthchecks.io или UptimeRobot, бесплатный тир, ~5 минут): падение туннеля сегодня — полный внешний отказ при зелёных внутренних алертах (аудит 05.08, Д-2), ADR-006 делает внешний uptime условием прода. Регистрация аккаунта — действие основателя; пинг-heartbeat из алертера — #229. Пошагово — [рунбук §6](runbooks/founder-actions-pilot-launch.md): 15 минут, ни ИП, ни прода не требует.
- **Ретеншн `stays`/`guests`/`guest_sessions` — решить юрпакетом**: spec 0032 сроки этих таблиц отложила, срока нет, PII гостей копится без предела (аудит 05.08, Г-8). Задан вопросом 8 письма юристу; ответ превращается в код одной задачей.
- **ИП** — регистрация; без юрлица не подписать договор с отелем, а обязанности оператора ПД возникают от обработки, не от оплаты. Ради реестра операторов ИП **не** нужен — закон предусматривает подачу по ИИН физлица (справка §3); шаги, ОКЭД и реальная цена — [рунбук §1](runbooks/founder-actions-pilot-launch.md), он же владелец этих чисел: бездоходный пилот всё равно стоит ежемесячных отчислений за себя — ВОСМС и СО возникают даже при нулевом доходе, ОПВ при нулевом доходе закон платить не обязывает, ОПВР — платёж за работника.
- **Приказ об ответственном за обработку ПД**; реквизиты ИП в опубликованные юрдокументы; проверка `docs/legal/` юристом — готовое письмо с восемью вопросами в [рунбуке §5](runbooks/founder-actions-pilot-launch.md).
- **Договор с пилотным отелем** (с DPA-разделом). Кандидат в новую строку раздела N.5, найденный при сверке 12.08: обязанность отеля выдать контакты затронутых гостей при утечке — Правила уведомления субъектов требуют от нас ФИО/ИИН/телефон, которых мы по минимизации не храним (справка §4.2, вопрос 6 юристу).
- **Подписка на обновления подзаконки МИИЦР** к закону о ПД (сайт министерства или карточка закона № 94-V на `adilet.zan.kz` — рассылка об изменениях): формы уведомления по ст. 10-1, «мер защиты по категориям» (ст. 25-1) и «порядка маскирования и хеширования» (ст. 23 п. 2) на 12.08.2026 ещё нет, а последние два способны задеть и малого оператора. Это единственное, что осталось от снятого дедлайна 25.08 ([справка](audits/2026-08-12-pd-operator-registry-note.md) §3): триггер «опубликована подзаконка» записан в ADR-006, но триггер без подписчика не сработает. 10 минут, ни ИП, ни прода не требует.
- **Сбор фактов для ADR-004** у IT-отдела отеля: доступ к Opera, планировщик отчётов, принтер стойки, Wi-Fi в лобби. Разблокирует #121 (черновик ADR-004). Готовое письмо — [рунбук §4](runbooks/founder-actions-pilot-launch.md); те же семь вопросов закрывают три развилки «основного потока выдачи доступа» из FOUNDER_REVIEW_QUEUE.
- **#189** — ограничить источники экшенов: Settings → Actions → General → «Allow *owner*, and select non-*owner*, actions» с галкой «Allow actions created by GitHub». Сейчас разрешён **любой** экшен любого автора (`allowed_actions: "all"`, проверено API 04.08), то есть третьесторонний экшен можно завести в CI молча — а исполняется он в том же job'е, где лежат SSH-ключ staging и право пуша в GHCR. Оба наших экшена от GitHub, CI не ломается. Массовый пин по SHA рассмотрен и отклонён — обоснование и правило «первый не-`actions/*` экшен пинится SHA с первого дня» записаны в комментарии `.github/dependabot.yml`. Найдено ревью PR #188.
- ~~#198 — защита `main`: approve переживает push~~ — **сделано основателем 05.08**: оба тумблера
  (`dismiss_stale_reviews`, `require_last_push_approval`) включены при `enforce_admins: true`
  (проверено API 05.08). Любой push после approve снимает approve, merge заблокирован до нового —
  условия правила 9 из дисциплины стали машинными. Найдено ревью PR #196; смежная развилка
  «кто щёлкает тумблеры» — #195.
- **#199** — конституция против правила 9: R-9 кончается фразой «всё, что CI не проверяет, не может
  быть блокером», а правило 9 объявляет блокером человеческое суждение о безопасности, изоляции
  тенанта и сохранности данных — том, чего CI сегодня не проверяет (мета-теста RLS нет — #182,
  проверки R-4 нет — #45). Оговорка правила 9 нужна и правильна: чинить надо текст FOUNDATION,
  а его владелец — основатель (R-11). Три варианта развилки — в самой issue; правка отдельным
  PR после merge #196. Найдено ревью PR #196 (третий проход), основание (а) правила 9.
- **#187** — включить secret scanning и push protection: тумблеры в Settings → Code security, конфигом в репозитории не делаются. Выключены на публичном репо, хотя для публичных бесплатны — это класс инцидента H1 (`.env` в чекауте). Найдено ревью PR #152; там же, в `FOUNDER_REVIEW_QUEUE.md`, — два соседних тумблера Dependabot.

---

## 3. Отложено осознанно — не блокирует пилот

Решение зафиксировано планом («Осознанно НЕ в Gate B») либо самой issue. Строка здесь —
не «забыли», а «посмотрели и решили после пилота». Возврат в раздел 1 — только явным решением.

**Устойчивость сборки — следствия #302 (порядок внутри раздела за основателем):** **#304** (переезд на `anthropic` SDK 1.x: мажор 1.0.0 от 20.08.2026 переехал с `httpx` на `httpx2`; сегодня нас держит граница `<1`, поставленная PR #303 — **Opus 5 `xhigh`**, уровень A: `ai/gateway` в красной зоне правила 9. Раздел 3 обоснован в теле issue: платформа работает на 0.x, ни одного из трёх оснований подъёма нет, а переезд красной зоны через сутки после мажорного релиза — худший из возможных моментов), **#305** (тот же класс поломки без привязки к SDK: 17 из 18 рантайм-зависимостей и 8 из 9 dev-зависимостей записаны как `>=` без верхней границы, лок-файла нет, и CI с `Dockerfile` берут с PyPI то, что там лежит в момент сборки — за месяц это выстрелило дважды, `ruff 0.16.0` 25.07 и `anthropic 1.0.0` 21.08, второй раз остановив мержи во всём репозитории. Развилка «лок-файл / верхние границы / чинить по факту» тянет ADR и решается основателем, не сессией — **Fable 5 `xhigh`**, уровень A: решение на момент задачи ещё не принято, а это класс Fable по правилу 7 (Opus 5 `xhigh` там оставлен за формализацией уже принятого решения). Ущерб каждого отдельного случая обратим и стоит одного окна, поэтому раздел 3; но цена растёт по мере приближения пилота, и пункт стоит посмотреть раньше прочих в этом разделе).

**Сделано без issue (правило 10 — плановая работа строкой с обоснованием):** PR #314 (Dependabot, `anthropic<1` → `<2`) закрыт 25.08 вердиктом ревью: обязательный `check` на нём красный, а починка — это и есть #304, робот её выполнить не может. Чтобы бот не приносил тот же красный PR на каждый релиз SDK и не занимал раздел 0 еженедельно, отдельным PR в `.github/dependabot.yml` добавлен игнор мажорных обновлений `anthropic` — по образцу игнора `ruff`, который там уже стоит. **Игнор снимается тем же PR, который закрывает #304**, иначе после переезда он молча запретит и мажор 2.x.

**Названо планом:** **#55** (защита от ошибочного `done`), **#82** (фото к заявке), **#83** (резолв `#N` при коллизии), **#138** (prompt caching — независимо, по ёмкости), **#90** (несколько нужд в одном сообщении), **#91** (гонка быстрых сообщений) — последние две «если проявятся».

**Названо самой issue:** **#117** (dedupe веб-чата — «для пилота приемлемо»), **#162** (RBAC v1, пер-тенантный отзыв — «не блокирует пилот, один отель»), **#53** (Prometheus multiprocess — Phase 1), **#35** (несрочный бэклог после 0017.1).

**Хвосты ревью и мелочи:** **#311** (реестр ПД по трём полям с PII **сотрудника** — `claimed_by_display_name`, `users.display_name`, `staff_invites.invited_name` — обещает «удаление по обращению субъекта — runbook subject-requests», а рунбук называется «обращения субъектов (гостей)» и по содержанию гостевой целиком: снапшотов имён персонала в нём нет ни одной строкой. Найдено пре-ревью PR #309 по оси PII; четвёртая строка `closed_by_display_name` того же обещания уже не даёт и ссылается на эту issue. Раздел 3 при том, что срок ответа субъекту — 3 рабочих дня: обращений от сотрудников нет и не может быть до найма персонала пилота, а разово ответить основатель способен руками. Задача не оформительская — `User` не удаляется вовсе, снапшот имени переживает деактивацию **по замыслу**, поэтому решать надо, что именно делать: обнулять снапшоты, заменять на «сотрудник N» или отвечать отказом по законному основанию. Последнее — правка юрпакета, и тогда уровень A — **Fable 5 `xhigh`**; если решение сведётся к разделу рунбука — **Opus 5 `high`**, уровень ревью B), **#310** (смоук-сценарий spec 0035 §13: заявка через `POST /api/v1/requests` с `origin=api` и чтение обратно — единственная проверка новых колонок на живом стенде. Схему завёл PR #309, сценарий в него не вошёл: карточка #298 называет три блока тестов §13, смоук — четвёртый, и таблица §14 его ни одному PR серии не приписывает; вдобавок смоук гоняется не в `make check`, а отдельным прогоном по staging, поэтому в PR #309 он приехал бы непроверенным. Раздел 3: колонки уже пишутся и уже покрыты интеграционными тестами, отсутствие смоука ничего не теряет безвозвратно — **Opus 5 `high`**, уровень ревью B), **#286** (два деплоя внахлёст: общий маркер снимка с фиксированным именем роняет тот деплой, который снимок как раз снял, — и в логе называет чужую причину, «устаревший `backup.sh` на сервере». Находка второго прохода ревью PR #284 о наложении деплоев, которого тот PR не трогал, — основания (б) и (в) правила 9. Раздел 3 обоснован в теле issue: отказ fail-closed, схема и данные БД не задеты, теряется один прогон деплоя на staging. Порядок правки важен: сериализовать деплой (`concurrency:` у job `deploy-staging`, `flock` в `deploy.sh`) — **раньше** уникального имени маркера, потому что сегодня общий маркер работает случайной блокировкой; снять её первой значит пустить два `alembic upgrade head` одновременно, а это уже раздел 1), **#288** (алертер может молча решить, что доложил: `evaluate()` переключает флаг состояния **до** отправки, а `send_telegram_message` на `httpx.HTTPError` только пишет `alert_send_failed` и идёт дальше — второго повода у алерта состояния не будет. Sentry в процессе алертера не поднят (`init_sentry` зовут только `app.py` и `worker.py`), так что запись остаётся строкой в логах контейнера; возвращает потерянный алерт только перезапуск — на staging это каждый деплой, на проде редко. Ревью PR #287, основание (б): и отправка, и порядок «переключить → отправить» пришли из Task 0018. Вес находке добавил как раз #106 — в набор теряемых попал ERR-OPS-008, а отсутствие бэкапа, в отличие от прочих линий, не проявляется ничем до дня аварии. Раздел 3 обоснован в теле issue: данные гостя и деньги не задеты, ущерб обратим, а Exit-критерий Gate B «уведомление службе не теряется молча» — про outbox/dead-letter, а не про доставку самих алертов), **#289** (каталог бэкапов на новом сервере может достаться `root`: у bind-mount'а короткого синтаксиса `create_host_path` включён по умолчанию, и отсутствующий каталог создаёт демон Docker — `root:root`, после чего `chmod o+rx` в `backup.sh` падает с EPERM (предупреждение, не стоп), а запись дампа — с `Permission denied`, и так каждую ночь. Штатный путь безопасен: `deploy.sh` создаёт каталог под `deploy` раньше `compose up`; остаётся один путь — новый сервер плюс `SKIP_PRE_MIGRATE_BACKUP=1` на первом же деплое, узко, но переезд прода в РК (#49) как раз впереди, а рычаг существует ровно для «с бэкапами что-то не так, а выкатить надо сейчас». Ревью PR #287, основание (б): чинится строкой в `ops/deploy/bootstrap-server.sh`, которого PR не трогает. Раздел 3 — отказ громкий на всех трёх выходах: ERR-OPS-008, ручной прогон `backup.sh`, следующий штатный деплой), **#290** (третий хвост того же второго прохода ревью PR #287, и потому строка стоит рядом с #288/#289. Две латентные находки в одном issue: (1) путь каталога бэкапов не имеет единой точки — `ops/backup/fetch.sh` держит его строкой трижды и `BACKUP_DIR` не читает, поэтому после переезда каталога по рецепту из `.env.staging.example` `make backup-fetch` встанет и назовёт ЛОЖНУЮ причину («бэкапы не настроены или падают?»), хотя они идут исправно; (2) `probe_backups` гасит `PermissionError` тем же `suppress(OSError)`, что и гонку с retention, — каталог, который читается, но не проходится (`o+r` без `o+x`), даёт ERR-OPS-008 с неверной причиной. Раздел 3 обоснован в теле issue: данные гостя и деньги не задеты, ущерб обратим, оба отказа громкие и самодиагностирующиеся — это не класс тихой лжи Н-1 первого прохода. Вердикт того прохода — approve, ветки уже нет, поэтому строку вписала следующая сессия (#208): правило 9, «при вердикте approve строку вписывает следующая сессия»), **#292** (ЧП-перехват не виден в кабинете: эскалация живёт только в Telegram, заявки по ЧП не создаётся — ночной портье, работающий в кабинете, о пожаре не узнает, и измерить ЧП нечем (#210). Смежная находка задачи #208, основание (а): какая служба владеет ЧП — продуктовое решение основателя, тянущее правку онбординга. Раздел 3 обоснован в теле issue: сигнал НЕ теряется — эскалация уходит в дефолтный staff-чат, гость получает телефон ресепшена; речь о видимости), **#293** (ЧП до согласия: гость, ещё не принявший условия, на «пожар» получает экран согласия — перехват стоит после consent-gate, потому что до согласия система не хранит даже текст сообщения (spec 0029). Смежная находка задачи #208, основание (а): развилка юридическая — три варианта в теле issue, формулировку и правку политики конфиденциальности утверждает основатель. Раздел 3: окно узкое — только первое касание гостя с ботом), **#294** (ЧП-перехват и снятие гейта P-9 без метрик: у обоих событий есть только логи (`emergency_intercepted`, `confirmation_gate_waived`), а счётчика `/metrics` нет — тогда как канон гостевого события его даёт (`record_guest_rate_limited`, `guest_consent_total`). Мерить нечем ровно те две величины, которые сам PR #291 назвал рисками: долю ложных срабатываний списка маркеров (spec 0034 §1) и долю снятых гейтов (ADR-018); алертер читает `/metrics`, а не логи. Туда же `EmergencyMatch.marker` — поле вычисляется и не читается ни разу: либо в лог, либо из dataclass вон. Находка ревью PR #291, метка «issue» — предложение улучшения, а не дефект (правило 9). Раздел 3 обоснован в теле issue: данные гостя и деньги не задеты, ущерб обратим, Exit-критерий Gate B не назван, сигнал не теряется — событие видно в структурном логе и в staff-чате), **#160**, **#163** (рекомендации ревью PR #155/#159), **#200** (реестр документов FOUNDATION §0 отстал от репозитория: роль `docs/runbooks/` у́же содержимого, нет восьми появившихся документов — ревью PR #197; чинится только осознанной правкой конституции, основание (а)), **#201** (там же, вторая половина расхождения `CLAUDE.md` с конституцией: четыре пункта «Ключевых фактов» пересказывают FOUNDATION без ссылки на владельца, хотя соседние ссылку несут — ревью PR #197, основание (б); правка доксовая, в один файл, поэтому берётся вместе с #200), **#173** (IP в `staff.cross_origin_rejected`), **#168** (кэш конфига тенанта при поллинге — ревью PR #170 добавило туда второе чтение на ход диалога), **#181** (хвосты ревью PR #170: тест-заглушка описания инструмента, `--slug` против `--tenant-slug`), **#23** (детали `ValidationError` в лог), **#47** (единый язык `AppError`), **#240** (`make queue` метит занятыми чужие пункты: диапазон номеров в заголовке PR — «(#206–#237)» — разбирается как список занятых issue, и верхний блокер очереди печатается «занято»; найдено ревью PR #238, дефект `ops/queue.sh`, которого тот PR не трогал — основание (б) правила 9), **#278** (тот же файл и тот же класс: после пуша доработки раздел 0 продолжает писать «доработка за автором», потому что `reviewDecision` пушем не сбрасывается — проверено живьём на PR #270; следующая сессия идёт дорабатывать уже доработанное, пока не откроет PR и не сверит даты руками. Чинится тем приёмом, который рунбук ревью §8 и так предлагает ревьюеру, — датой последнего ревью против даты последнего коммита; та же правка закрывает и «approve есть — мержить» после push'а, снявшего approve (#198). Найдено при доработке PR #270, основание (б); место в порядке работ — за основателем, правило 10), **#280** (третий из того же класса и тот же `ops/queue.sh`: две слепые зоны сверки, где «расхождений нет» читается как «всё сходится». Вычеркнутый пункт (`~~#N~~`) под проверки не попадает — он не жирный, значит не пункт, а номер в файле упомянут, значит и вторая проверка молчит: строка «~~#46~~ закрыт PR #270» стояла с 13.08 при открытом на GitHub #46, и сверка это пропускала. Второе — блок процесс-батча правила 11 печатает «СОЗРЕЛ… берёт увидевшая», не заглядывая в уже собранный скриптом список занятых, так что заявка на батч draft-PR'ом соседней сессии не видна там, где сессия смотрит. Найдено вторым проходом ревью PR #277: первая зона старше PR, вторая — про стык нового блока с нетронутым разбором «занято», основание (б); берётся одним PR с #240 и #278), **#257** (обе метрики outbox считаются одним `count(*) FILTER` без ограничивающего `WHERE` — seq scan таблицы на каждый scrape `/metrics`, раз в минуту и вместе с 30-дневным ретеншном; два отдельных `count(*)` держат условия на индексах — ревью PR #256, на объёме Фазы 0 разница в миллисекундах), **#258** (две копии формулировки «алерт о dead-letter приходит ровно один раз на событие» — в `.env.example` и докстринге `tests/test_outbox_alerts.py`; после ADR-015 верно «на каждые похороны», и обе копии молча разошлись с владельцем факта — ревью PR #256), **#261** (нет машинного щита от docs-drift H-2: env сервисов в обоих compose перечислено поимённо, и новая настройка `Settings` молча остаётся в контейнере дефолтом — так было с `TELEGRAM_ALERT_*` в #133 и с `WORKER_DELIVERY_LEASE_SECONDS` в #134; тест, сверяющий `.env.example` с env сервисов обоих compose, закрывает класс целиком — ревью PR #259, предложение улучшения), **#263** (тот же класс, найденный руками: `WORKER_RETENTION_INTERVAL_SECONDS` и `WORKER_REMINDER_INTERVAL_SECONDS` описаны в `.env.example`, но в env сервиса `worker` не пробрасываются ни в одном из compose — правка на сервере молча ничего не меняет; найдено при #136, две строки в два файла), **#264** (алертер слепнет молча: не-200 или таймаут на `/metrics` при живом `/health/ready` схлопывается в те же `None`, что и «метрика пуста», — обе линии #136 и всплеск 5xx замолкают, и никто не говорит, что watchdog перестал видеть; до #136 через этот канал шёл вторичный сигнал, теперь — единственная линия обнаружения мёртвого воркера — ревью PR #262, основание (в): нужен свой код **ERR-OPS-007** — 006 занят бюджетным алертом (PR #265), а номера каталога не переиспользуются (`docs/runbooks/errors.md`, «Правила»); плюс своя ветка машины состояний и признак «эндпоинт не ответил» отдельно от «метрика пуста»), **#268** (`llm_call_log` на исходах timeout/error пишет `settings.llm_model`, а не модель фактического провайдера: `LlmProviderResult` там ещё не существует, и `result.model` взять неоткуда. Сегодня источники совпадают — провайдера мимо настроек собирает только bake-off; там же и проявится: таймауты кандидата лягут в журнал под моделью из `.env`, то есть под чужой, и испортят ровно то сравнение, ради которого bake-off запускают. Честная починка — объявить `model` в протоколе `LlmProvider` рядом с `name` и обновить оба Fake-адаптера: контракт порта и тестовый слой, основания (б) и (в). Ревью PR #266), **#272** (`ai/gateway/service.py` вырос с 355 до 515 строк, `complete()` — около 100 при границе R-3 «файл ≤ ~400 строк, функция ≤ ~50»; напрашивающийся шов назван в issue — вынести бюджет с резервом в `ai/gateway/budget.py`, публичный контракт `api.py` при этом не меняется. Ревью PR #270, предложение улучшения, а не дефект: длину файла CI не проверяет, значит блокером она быть не может (R-9). Брать стоит **классом, а не одним файлом**: за границей R-3 сегодня не один этот файл, а несколько — перечень с размерами держит тело issue, здесь его нет намеренно (числа растут с каждым PR, и копия устаревает молча). Разрезать их по одному отдельными PR дороже, чем одним заходом с общим приёмом разделения), **#273** (хвост резерва бюджета #46, **осталась половина**: снятие резерва не проверено ассертом на исходах `timeout` и `error` — структурно их покрывает `finally`, но перенос снятия внутрь `try` эти тесты не покраснят; лечится строкой `assert await _reservations(tenant_a) == []` в конце двух существующих тестов. Ревью PR #270, предложение улучшения. Вторая половина — `LLM_MAX_ATTEMPTS=0` ронял `complete()` на `assert result is not None` — **закрыта PR #282** тем же приёмом fail-fast конфигурации, что и #267: `Field(default=3, ge=1)` в `shared/config.py`, и `max(..., 1)` в `_reservation_lease_seconds` за ненадобностью снят. Места #272 и #273 — решение 13.08, делегированное основателем сессии-ревьюеру, [FOUNDER_REVIEW_QUEUE](FOUNDER_REVIEW_QUEUE.md)), **#279** (сюда же и по той же причине: `.env.example` называет паузы между попытками расчётными «~1 с и ~2 с», то есть до применения разброса, — четвёртая копия числа, владелец которого статья ERR-AI-001; правка в одну строку, неверного действия из комментария не следует. Находка второго прохода ревью PR #270 о нетронутой части diff — основание (б); берётся вместе с #272/#273, это хвосты одного PR), **#283** (числовые поля `Settings` без границ: приём «граница на самом поле» покрывает единицы числовых полей, остальные принимают 0 и отрицательные молча (счёт и список — в теле issue). Часть даёт не «странную настройку», а отказ: `messages_retention_days=0` сносит переписку гостей целиком следующим проходом ретеншна — необратимо и это данные гостя (spec 0032), `worker_batch_size=0` останавливает доставку без единой ошибки, `alert_poll_interval_seconds=0` загоняет watchdog в busy-loop. Ревью PR #282, основание (б): этих полей PR не трогал. Брать классом целиком — цена одного PR почти вся в цикле ревью, а не в строках; попутно отметить комментарием поля, где 0 — законное «выключено», иначе следующий заход поставит границу по инерции), **#285** (в `backup.sh` проверка «архив читается» (`pg_restore --list`) держится только на `set -euo pipefail`, тогда как три соседние проверки годности дампа — явными `if`. В поведении дыры нет, дыра в щите: все тесты `tests/test_pre_migrate_backup.py` остаются зелёными на `backup.sh`, из которого убран `set -e`, — тот самый класс, за который репозиторий уже заплатил в PR #282. Ревью PR #284, основание (б): этой строки тот PR не трогал. Раздел выбран ревьюером и обоснован там же — инвариант сегодня цел, под три обоснования подъёма в раздел 1 находка не подпадает), **#184** и **#186** (рекомендации ревью PR #152: job `audit` ставит dev-инструменты, которых нет в проде; базовый образ `python:3.12-slim` не покрыт Dependabot'ом), **#193** и **#194** (хвосты ревью PR #192: висячая отсылка в разделе 0 этого файла и три неточности разноски; правило «первый не-`actions/*` экшен пинится SHA» записано в комментарии `dependabot.yml`, а применяет его сессия, редактирующая workflow) — под новым умолчанием правила 9 обе чинились бы в самом #192, но он смержен; переразметка задним числом — решение основателя (FOUNDER_REVIEW_QUEUE, 04.08), ~~#191~~ (правило 9 не предусматривало PR от робота — закрыт PR #192: фраза в правило 9 и п. 6 рунбука ревью), **#276** (`make review` — headless-ревьюер одной командой, после разгрузки очереди, стратсессия 13.08; правило 9 не меняет — чистый контекст сохраняется, автоматизируется только запуск окна. **Сам PR — уровень A** (цель Makefile, канонический промпт ревьюера, работа с конфигом второго аккаунта — инструменты и конфигурация процесса; сомневаешься — уровень A); «уровень B» в issue относится к **пилоту**: первые 3–5 прогонов ставятся на PR уровня B, где цена пропуска ниже. Ограничение известно заранее: `claude -p` не запускает workflow по слову `ultracode` (правило 7), поэтому многоагентный прогон, рекомендованный уровню A, headless-ревьюеру недоступен — комментарий в issue), **#296** (экран согласия и вопрос «что не сделано?» теряются при сбое Bot API: дожим #209 уводит в outbox только реплики через `send_reply`, а эти два вызова шлют напрямую — им нужен `reply_markup`, которого событие `telegram_reply.undelivered` не несёт. Смежная находка автора PR #295, умолчание правила 9 — раздел 3. Обоснование в теле issue: молчаливой безвозвратной потери, которую чинил #209, здесь нет — экран согласия сам покажется на следующем сообщении гостя (ключ пишется только после успешной отправки), а потерянное нажатие «Готово частично» сотрудник повторяет кнопкой, заявка остаётся `in_progress`. В коде помечено комментарием со ссылкой на issue), **#306** (сводка дня: копия основателю не защищена от повтора ничем — `shared/alerting.py` шлёт прямой `sendMessage` и строки `messages` не пишет, а вся идемпотентность канала висит на уникальном `(tenant_id, idempotency_key)` именно там. Пока `daily_summary_chat_id` тенанта заполнен, копию неявно прикрывает ключ отельной сводки; но [spec 0035](specs/0035-pilot-measurability.md) §8 объявляет пустой `daily_summary_chat_id` рабочим состоянием — тогда ключ не пишется никогда, а условие «наступило локальное время» истинно с 09:00 до полуночи, и копия уходит в платформенный алерт-чат ~90 раз за сутки, топя ERR-OPS-*, dead-letter и свежесть бэкапов. Находка второго прохода ревью PR #297 о нетронутой части diff — умолчание правила 9, раздел 3: merge не блокирует, данные гостя и деньги не задеты, отказ шумный. Работа уезжает внутрь **#301** — спека обязана назвать носитель «ровно одного раза» для копии, иначе сессия #301 напишет то, что покажется ей естественным)), **#307** (сводка дня: «Просрочено за день» считает просроченными до-миграционные заявки. Предикат [spec 0035](specs/0035-pilot-measurability.md) §6 у невзятой заявки (`claimed_at IS NULL`) меряет срок до отсечки `min(конец суток, момент сводки, closed_at)` — и опирается на то, что закрытие у невзятой означает отмену. Для строк после миграции 0025 это верно, а бэкфилл §3 проставляет `closed_at` всем терминальным строкам и `claimed_at` не восстанавливает ничем: у взятой и выполненной до миграции заявки оба поля выглядят как у брошенной, и она встаёт в сводку просроченной. Заодно устарели две формулировки самой спеки — абсолют «закрытие у невзятой всегда означает отмену» (§6) и «цена честного NULL — одна» (§3): цен две. Находка третьего прохода ревью PR #297 о нетронутой части diff — умолчание правила 9, раздел 3: merge не блокировала, данные гостя и деньги не задеты, ущерб обратим (число пересчитывается запросом), а Exit-критерий Gate B меряет долю `origin = guest_chat`, не просрочку. Радиус узкий: задеты только дни до миграции 0025, данных пилота среди них нет, на staging завышено число за день выката и видно оно сутки. Работа уезжает внутрь **#300** — там пишется сам SQL сводки; предложенная починка — во второй ветке предиката добавить `AND status = 'cancelled'` рядом с отсечкой `closed_at`, тогда закрывается и бэкфилл, и исходная дыра Н2-1), **#312** (приём «`SET LOCAL row_security = off` перед DML тенантной таблицы» записан только внутри одноразовой миграции 0025, а копируют новые миграции канон `0002_tenant_rls_canon.py`, где про бэкфилл тенантной таблицы нет ни слова. Под ролью без BYPASSRLS такой UPDATE обновляет ноль строк молча — воспроизведено ревью PR #309 под `NOSUPERUSER NOBYPASSRLS`, — и следующий автор бэкфилла заведёт ту же тихую дыру заново. Находка первого прохода ревью PR #309, раздел 3: нынешняя роль миграций — суперпользователь докерного образа, то есть ни на staging, ни в CI дыра сегодня не стреляет — **Opus 5 `high`**, уровень ревью B), **#313** (знаменатель доли Exit-критерия определён в spec 0035 дважды и по-разному: §4 — `guest_chat` ко ВСЕМ заявкам отеля, §6 — `guest_chat` и `staff_manual` как «числитель со знаменателем»; расходятся на `api`, а §9 закрепляет двузначную арифметику прямо в тексте менеджеру («47 создано… 39 через бота, 8 вручную»). Второй пункт issue — смоук создаёт заявки Telegram-вебхуком, то есть AI-инструментом с `origin = guest_chat`, и после перевода post-deploy smoke на тенант пилота они попадут прямо в числитель. Находка второго прохода ревью PR #309; §6 и §9 реализует PR D (**#300**), поэтому расхождение снимается отдельным пунктом и обязано встать раньше #300, а не внутри него. Развилка второго пункта (строка рунбука «смоук не гоняем по тенанту пилота» против колонки-признака в БД) — за основателем, и от неё зависит цена: правка двух разделов спеки и строки рунбука — **Opus 5 `high`**, уровень ревью B; признак синтетики в БД — миграция и влияние на измеряемое число, то есть уровень A и **Fable 5 `xhigh`** (правило 7: развилка не принята)).

**Решено, ждёт триггера:** **#275** — процесс-батч: копилка правок правил, рунбуков и косметики (правило 11) — **Opus 5 `xhigh`** (уровень A: правила исполнения, CLAUDE.md, рунбуки процесса). Строка постоянная — её не вычёркивают после батча, копилка живёт дальше. **Триггер — порог, а не календарь** (числа у владельца — правило 11): считает `make queue` и печатает «созрел / не созрел» рядом с разделом 0, вручную проверять не нужно. Созревший батч берётся взамен нового пункта раздела 1, а не в дополнение к нему (правило 10), одним PR, с вычёркиванием внесённых строк в самом issue. **#190** — смоук самопропускается на PR от Dependabot (секреты репозитория ботовым PR не подставляются, guard видит пустой `ANTHROPIC_API_KEY` и зеленит job, ничего не проверив) — **Opus 5 `high`**. Решение основателя 04.08: боевой ключ в Dependabot-секреты **не** дублируется — на ботовом PR исполняется свежий чужой код, и ключ оказался бы рядом с ним; вместо этого отдельный ключ CI в своём workspace с месячным потолком трат (потолок per-workspace — [док Anthropic](https://platform.claude.com/docs/en/api/rate-limits), на дефолтном workspace он недоступен), плюс громкий маркер «смоук не гонялся». Триггер работы — появление лок-файла: пока бот трогает только `.github/workflows/`, смоук там нечего проверять; на PR с обновлением FastAPI/SQLAlchemy он станет главным пропущенным сигналом. До тех пор ревьюер смотрит прогон на main после merge ([рунбук ревью](runbooks/review-process.md), п. 6). **#260** — просроченная аренда outbox позволяет старому диспетчеру перезаписать исход чужой доставки: запись опоздавшего затирает свежую аренду второго воркера, а на последней попытке может похоронить событие, которое второй успешно доставил (ложный алерт ERR-EVENTS-002 человеку, +1 к `outbox_dead_letter_events`) — **Opus 5 `xhigh`** (правка канона доставки). Триггер работы — второй одновременно живущий экземпляр воркера: сегодня он один (ADR-005), и окно недостижимо — при деплое старый контейнер останавливается задолго до истечения 300-секундной аренды. Рецепт готов в самой issue (fencing: `_record_outcome` пишет с условием `locked_until = <своя аренда>`, `rowcount = 0` — в лог), там же второй пункт того же класса из «Пересмотреть» ADR-016 — продлевать аренду перед каждым событием пачки, а не один раз на пачку. Найдено ревью PR #259.

**Продуктовые, требуют решения:** **#58** (частичное выполнение — решение не принято; вопрос «список или текст» общий с #54, вынесен в FOUNDER_REVIEW_QUEUE), **#92** (одна заявка = одно сообщение в staff-группе), **#89** (карта деградаций).

**Проверить на перекрытие:** ~~#18~~ (outbox retention + backoff) — проверено и закрыто вместе с #133 (PR #256): backoff (`next_attempt_at`) и retention обработанных строк были реализованы ещё в ADR-009 и коде, issue осталась открытой по недосмотру; retention исчерпанных строк добавлен #133. Остатка скоупа нет.

**Аудит 05.08, «можно после пилота»:** **#235** (мелочи staff-поверхностей: статусы по-русски, протухшие клавиатуры, адресность отмены, навигация), **#236** (AI-слой: stop_reason, язык на диалоге, атрибуция стоимости, окно истории), **#237** (домен: обмен комнатами занятых Stay, initiator в `/api/v1`). Переоценок мест у аудита три, все — комментариями в самих issues, решает основатель: #83 (Е-2) и #92 (Е-3) стоят в этом разделе, #101 (Е-4) — уже в разделе 1, группа «Качество и полнота проверок».

---

## Связанные документы, которые очередью **не** являются

- [PROJECT_EXECUTION_PLAN.md](../PROJECT_EXECUTION_PLAN.md) — зачем и по каким критериям; Exit Gate B; правило исполнения 10 — нормативная механика этой очереди.
- [DISCUSSION_LOG.md](DISCUSSION_LOG.md) «Готовность к пилоту» — карта целого, чтобы видеть, из чего пилот состоит. Порядок работ там не живёт.
- [FOUNDER_REVIEW_QUEUE.md](FOUNDER_REVIEW_QUEUE.md) — «прочитай и согласись/возрази»: решения, принятые LLM от имени основателя. Не задачи.
