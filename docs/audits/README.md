# Аудиты фазы

Независимые аудиты, проводимые отдельными LLM-сессиями с чистым контекстом
(правило исполнения 8 в `PROJECT_EXECUTION_PLAN.md`: Exit Criteria каждой фазы
включают такой аудит). Отчёты сюда кладутся как есть — это **запись состояния на
дату**, а не живой документ: их не переписывают задним числом, найденное
превращают в issue/ADR (R-11).

## Именование

`ГГГГ-ММ-ДД-<тема>-audit.md` — дата проведения, не дата коммита.

## Аудиты Phase 0 (21–23.07.2026)

Семь прогонов, которые по правилу 8 **и есть** аудит фазы 0:

| Файл | Тема |
|---|---|
| `2026-07-21-security-audit.md` | безопасность (+ аддендум седьмого прогона, PR #95) |
| `2026-07-21-operational-audit.md` | операционная зрелость |
| `2026-07-22-legal-audit-rk.md` | юридический контур РК |
| `2026-07-22-legal-recommendations.md` | юррекомендации (спутник предыдущего) |
| `2026-07-23-ai-quality-audit.md` | качество AI |
| `2026-07-23-docs-drift-audit.md` | дрейф документации ↔ код |
| `2026-07-23-optimization-audit.md` | оптимизация |
| `2026-07-23-token-economics-audit.md` | экономика токенов |

Сводный вердикт и вытекающий план — `PROJECT_EXECUTION_PLAN.md`, раздел
«Мост Phase 0 → Phase 1» (Gate A / Gate B).

## Аудит Gate B (05.08.2026)

Прогон другого жанра, чем отчёты Phase 0 выше: не проверка Exit Criteria фазы
(правило 8), а запрос основателя — «просканировать каждый миллиметр, поставить
себя на место персонала отеля» перед пилотом на 310 номеров.

| Файл | Тема |
|---|---|
| `2026-08-05-operational-audit.md` | операционная готовность к пилоту (§3–§8 находки, §9 — что проверено и крепко), рекомендации по функционалу (§11), решения основателя (§12) |

Разнесён 06.08.2026 (PR #238): issues #206–#237, две группы раздела 1 в
`../QUEUE.md`, запись в `../DISCUSSION_LOG.md`. Порог запуска, вытекающий из §12
п. 1, — «Exit Gate B» в `PROJECT_EXECUTION_PLAN.md`.

## Историческая сноска про путь

Операционный аудит изначально был записан в `docs/audit/OPERATIONAL_AUDIT.md`
(каталог в единственном числе — так просил его промпт). Каталог унифицирован в
`docs/audits/`, файл живёт как `2026-07-21-operational-audit.md`. Ссылки на
старый путь встречаются внутри отчётов и промптов — они оставлены нетронутыми
намеренно (запись того, что видел аудитор), читать их следует через эту сноску.

## `prompts/`

Промпты, которыми запускались аудиты. Хранятся ради воспроизводимости: правило 8
требует такой аудит на выходе **каждой** фазы, и следующий прогон начинается с
копии соответствующего промпта, а не с чистого листа. Это тоже исторические
записи — правится копия под новую фазу, а не архив.

**Пробел, закрытый частично.** Промпт аудита безопасности 21.07 не сохранился —
и это как раз тот промпт, потеря которого дороже всего. Не сохранился и промпт
юридического аудита РК: в каталоге пять промптов на семь прогонов. Восстановить
его нельзя. `security-audit-prompt.md` — не он: это промпт следующего прогона,
собранный 30.07 из метода, описанного в самом отчёте, плюс две оси, которых у
прогона 21.07 не было (авторизация на уровне объекта / IDOR и приватность
данных). Файл честно помечен внутри; путать его с исторической записью нельзя.

**Аудит — не сканер.** Отчёт фиксирует состояние **на дату** и **по осям**: что
не было осью, то не проверялось, а код с тех пор ушёл вперёд. Поэтому
«аудит был» ≠ «код проверен» — прежде чем опираться на отчёт, сверься с
`git log <хэш предмета аудита>..main`. Класс, который чтением кода не находится
(известные CVE в зависимостях), автоматика **замечает**: job `audit` в CI
(`pip-audit`, локально `make audit`). Ремедиация автоматикой не закрыта:
Dependabot приносит фиксы только по жёстким пинам, а у проекта их нет ни одного
(кроме `ruff`, который двигать нельзя) и нет лок-файла — почему, разобрано в
`.github/dependabot.yml`. Найденное сканером обновляется руками.
