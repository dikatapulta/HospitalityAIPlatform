"""Evals AI-слоя (Task 0015, FOUNDATION §7.7).

Два артефакта:
- CI golden-set v0 (структурный, на mock) — в `ai/tests/test_golden_set_v0.py`:
  детерминированно гейтит merge, язык не значим.
- `bakeoff.py` — офлайн-прогон на РЕАЛЬНЫХ моделях (Haiku 4.5 vs Sonnet 5) на 6
  языках пилота (ADR-010): фиксирует `LLM_MODEL` по качеству. Не в CI (стоит
  токены, недетерминирован), исключён из покрытия.
"""
