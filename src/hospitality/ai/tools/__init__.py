"""AI-инструменты (Task 0015, FOUNDATION §7.3, P-5).

Каждый инструмент — тонкая декларативная обёртка над сервисом ядра: имя,
типизированные вход/выход, класс подтверждения (P-9), какие модули зовёт.
AI не умеет ничего «эксклюзивно» — тот же сервис доступен через UI и API.

Вход для оркестратора — `registry` (сборка `ToolSpec`, класс подтверждения,
диспетчер `execute`). Канонический инструмент — `create_service_request`.
"""
