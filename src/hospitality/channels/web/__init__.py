"""Канал web — гостевой веб-чат по QR (spec 0027 §3, issue #79, ADR-008).

Строгий auth-only с рождения (Q7): без валидной GuestSession — статический
ответ без единого вызова LLM. Транспорт — HTTP JSON + одна статическая
страница; ход гостя — общий `channels/common/guest_turn`.
"""
